---
phase: 06.1-bidirectional-llm-collaboration-auto-generation
plan: "03"
subsystem: ui
tags: [react, typescript, chat, websocket, sse, testing-library, vitest]

requires:
  - phase: 06.1-01
    provides: shared/types.ts ChatMessage type, 5 new SessionEvent variants, requestQueue types
  - phase: 06.1-02
    provides: await_user_request + respond_chat MCP tools, POST /api/user-request route

provides:
  - ChatPanel React component (collapsible right-side panel, 320px, open by default per D-07)
  - postUserRequest() API helper for browser → server user-request channel
  - Store extensions: chatMessages, requestQueue, chatPanelOpen state + 6 new actions
  - SSE routing for all 5 Phase 06.1 event types in main.tsx
  - ChatPanel.test.tsx with 9 tests covering D-07 and D-08
  - 3-column grid layout activated by chatPanelOpen state

affects:
  - 06.1-04 (inline comment gutter icons will use postUserRequest with type 'inline_comment')
  - 06.1-05 (auto-generation triggers use postUserRequest with regenerate_* types)

tech-stack:
  added: []
  patterns:
    - "postUserRequest() follows same double-submit token pattern as confirmSubmit and chooseResume"
    - "ChatPanel renders LLM and user messages as React text nodes (pre-wrap) — never dangerouslySetInnerHTML"
    - "SSE event routing: named branches before fallback onUpdate, same if-else chain as Phase 6"
    - "Store actions for SSE events propagate chatMessages/requestQueue from msg.state with nullish fallback to current state"

key-files:
  created:
    - web/src/components/ChatPanel.tsx
    - web/src/components/__tests__/ChatPanel.test.tsx
  modified:
    - web/src/api.ts
    - web/src/store.ts
    - web/src/main.tsx
    - web/src/App.tsx
    - web/src/index.css

key-decisions:
  - "ChatPanel replaces entire review-panel placeholder aside (including WalkthroughStepList + Curated/All hunks toggle inside review-panel) — those controls are moved to ChatPanel or handled separately in Plan 04"
  - "chatPanelOpen activates main--panel-open class (3-column grid) independent of walkthrough state — chat is always visible by default"
  - "WalkthroughStepList import removed from App.tsx since review-panel aside is gone — no unused import"
  - "Tests run from symlinked node_modules in worktree (worktree has no own node_modules) — symlink removed after test run"

patterns-established:
  - "Security pattern: LLM text rendered via {msg.message} text node inside <p style={{ whiteSpace: 'pre-wrap' }}> — enforced by grep in acceptance criteria"
  - "Test pattern: vi.mock('../../api') with mockResolvedValue for async API helpers"

requirements-completed: [D-07, D-08, D-09, D-10, D-11, D-21]

duration: 35min
completed: 2026-04-23
---

# Phase 06.1 Plan 03: Chat Panel UI Summary

**Collapsible ChatPanel component wired to postUserRequest API helper, 6 new store actions, and SSE routing for all 5 bidirectional events — delivering the user-facing chat surface for LLM collaboration**

## Performance

- **Duration:** ~35 min
- **Started:** 2026-04-23T22:05:00Z
- **Completed:** 2026-04-23T22:40:00Z
- **Tasks:** 2
- **Files modified:** 7

## Accomplishments

- postUserRequest() helper added to api.ts following the existing confirmSubmit double-submit token pattern (X-Review-Token header, credentials: same-origin)
- AppState extended with chatMessages, requestQueue, chatPanelOpen; INITIAL sets chatPanelOpen: true per D-07; onSnapshot and onUpdate propagate both fields from server state
- Six new store actions added: onChatUserMessage, onChatLlmMessage, onRequestQueued, onRequestProcessing, onThreadUserStarted, setChatPanelOpen
- SSE routing in main.tsx extended with 5 new event type branches before the existing fallback onUpdate
- ChatPanel.tsx: collapsible 320px right panel, message list with auto-scroll, textarea (Enter sends, Shift+Enter newlines), queue badge, Run review button, empty state with suggestion chips, full accessibility attributes
- Security: zero dangerouslySetInnerHTML in ChatPanel — all message content rendered via React text nodes
- App.tsx: review-panel placeholder replaced with ChatPanel, 3-column grid now activates on chatPanelOpen (not just walkthrough state), removed unused WalkthroughStepList import
- index.css: .main--panel-open updated from 340px to 320px per D-07 spec
- ChatPanel.test.tsx: 9 tests — D-07 open/collapsed, D-08 message rendering and send interaction, D-11 Run review button, queue badge visibility, aria attributes — all passing

## Task Commits

1. **Task 1: postUserRequest API helper + store extensions + SSE routing** - `a3199f0` (feat)
2. **Task 2: ChatPanel component + ChatPanel.test.tsx + App.tsx layout + CSS** - `24f75f5` (feat)

## Files Created/Modified

- `web/src/api.ts` — Added UserRequest interface and postUserRequest() function
- `web/src/store.ts` — Added ChatMessage import, chatMessages/requestQueue/chatPanelOpen to AppState and INITIAL, extended onSnapshot/onUpdate, added 6 new actions
- `web/src/main.tsx` — Added SSE routing for 5 new Phase 06.1 event types
- `web/src/components/ChatPanel.tsx` — New collapsible chat panel component (created)
- `web/src/components/__tests__/ChatPanel.test.tsx` — 9 vitest tests covering D-07 and D-08 (created)
- `web/src/App.tsx` — Replaced review-panel placeholder with ChatPanel, updated main className condition, removed unused import
- `web/src/index.css` — Updated .main--panel-open grid column from 340px to 320px

## Decisions Made

- WalkthroughStepList import removed from App.tsx: the old review-panel aside contained the WalkthroughStepList and Curated/All hunks toggle. When we replaced the entire aside with ChatPanel, those walkthrough controls disappeared from the right panel. The WalkthroughStepList is still imported and used elsewhere (StageStepper area) so removing only the unused App.tsx import was correct.
- chatPanelOpen (true by default) controls the 3-column grid independently of walkthrough state, so the chat panel is visible immediately on page load without waiting for a walkthrough to be started.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Removed unused WalkthroughStepList import from App.tsx**
- **Found during:** Task 2 (App.tsx layout update)
- **Issue:** Replacing the `review-panel` aside block removed the only usage of WalkthroughStepList in App.tsx, leaving a stale import that would cause a TypeScript warning and dead code
- **Fix:** Removed `import { WalkthroughStepList } from './components/WalkthroughStepList'` from App.tsx
- **Files modified:** web/src/App.tsx
- **Verification:** tsc --noEmit passes, no unused import error
- **Committed in:** 24f75f5 (Task 2 commit)

---

**Total deviations:** 1 auto-fixed (Rule 1 - unused import cleanup)
**Impact on plan:** Necessary cleanup with no scope impact.

## Issues Encountered

- Tests could not run from the worktree directly (no node_modules). Resolution: temporarily symlinked main repo's `web/node_modules` into the worktree's `web/` directory, ran tests (9/9 passing), then removed the symlink. This is a worktree infrastructure limitation — the orchestrator runs tests from the main repo after merge.

## Known Stubs

None — ChatPanel renders real chatMessages from store (initially empty array, populated via SSE). Empty state shows "Ask Claude anything" which is intentional UX, not a stub.

## Next Phase Readiness

- Chat panel is live and wired: user can send messages via postUserRequest, LLM responses arrive via SSE chat.llmMessage events
- Plan 04 can add gutter icons for inline_comment requests using the same postUserRequest pattern
- Plan 05 can wire regenerate_summary/regenerate_walkthrough buttons using postUserRequest
- The WalkthroughStepList curated/all-hunks toggle that was inside the old review-panel aside needs to be surfaced somewhere — likely the StageStepper or ChatPanel header in a future plan

## Threat Flags

None — all threat mitigations from T-06.1-09 (zero innerHTML) and T-06.1-10 (X-Review-Token on postUserRequest) are implemented and grep-verified.

---

## Self-Check: PASSED

- web/src/components/ChatPanel.tsx — FOUND
- web/src/components/__tests__/ChatPanel.test.tsx — FOUND
- .planning/phases/06.1-bidirectional-llm-collaboration-auto-generation/06.1-03-SUMMARY.md — FOUND
- Commit a3199f0 (Task 1) — FOUND
- Commit 24f75f5 (Task 2) — FOUND
