---
phase: 06.1-bidirectional-llm-collaboration-auto-generation
plan: 05
type: execute
wave: 5
depends_on: [06.1-02, 06.1-03, 06.1-04]
files_modified:
  - commands/pair-review.md
autonomous: false
requirements: [D-01, D-02, D-03, D-17]

must_haves:
  truths:
    - "Slash command instructs Claude to auto-generate summary + walkthrough on startup"
    - "Slash command instructs Claude to enter the await_user_request listen loop"
    - "--dry flag skips auto-generation but still enters the listen loop"
    - "allowed-tools lists all 12 MCP tools"
    - "Loop discipline is explicit: Claude must never exit the loop"
  artifacts:
    - path: "commands/pair-review.md"
      provides: "Rewritten slash command with auto-generation flow + listen loop + --dry support"
      contains: "await_user_request"
  key_links:
    - from: "commands/pair-review.md"
      to: "server/src/mcp/tools/await-user-request.ts"
      via: "Slash command instructs Claude to call await_user_request in a loop"
      pattern: "await_user_request"
    - from: "commands/pair-review.md"
      to: "server/src/mcp/tools/respond-chat.ts"
      via: "Slash command instructs Claude to call respond_chat when processing chat requests"
      pattern: "respond_chat"
---

<objective>
Rewrite the slash command (commands/pair-review.md) to orchestrate the full Phase 06.1 flow: auto-generate summary + walkthrough on startup, then enter the indefinite await_user_request listen loop. Add --dry flag support. Expand allowed-tools to all 12 MCP tools. Conclude with a human-verify checkpoint to test the round-trip.

Purpose: This is the orchestration surface that makes the bidirectional model work. The slash command is the prompt that tells Claude how to behave as a co-reviewer: generating content automatically, then listening for user requests indefinitely.

Output: Rewritten commands/pair-review.md with auto-generation flow, listen loop protocol, and --dry support. Human-verified end-to-end round-trip.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06.1-bidirectional-llm-collaboration-auto-generation/06.1-CONTEXT.md
@.planning/phases/06.1-bidirectional-llm-collaboration-auto-generation/06.1-RESEARCH.md
@.planning/phases/06.1-bidirectional-llm-collaboration-auto-generation/06.1-PATTERNS.md
@.planning/phases/06.1-bidirectional-llm-collaboration-auto-generation/06.1-01-SUMMARY.md
@.planning/phases/06.1-bidirectional-llm-collaboration-auto-generation/06.1-02-SUMMARY.md
@.planning/phases/06.1-bidirectional-llm-collaboration-auto-generation/06.1-03-SUMMARY.md
@.planning/phases/06.1-bidirectional-llm-collaboration-auto-generation/06.1-04-SUMMARY.md

<interfaces>
The current commands/pair-review.md has YAML frontmatter with description, argument-hint, and allowed-tools (only start_review). The body instructs Claude to call start_review with source arg from $ARGUMENTS, then share the summary. This will be FULLY rewritten. Read the current file before writing.

Key references for the rewrite:
- RESEARCH.md Pattern 5: full slash command template with all 12 tools, startup sequence, listen loop
- PATTERNS.md pair-review.md section: frontmatter + body structure
- CONTEXT.md D-01 (auto-gen), D-02 (--dry), D-03 (auto-enter loop), D-17 (12 tools)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite slash command with auto-generation + listen loop + --dry</name>
  <files>commands/pair-review.md</files>
  <read_first>
    - commands/pair-review.md (current file)
    - .planning/phases/06.1-bidirectional-llm-collaboration-auto-generation/06.1-RESEARCH.md (Pattern 5: Slash Command Rewrite section)
    - .planning/phases/06.1-bidirectional-llm-collaboration-auto-generation/06.1-PATTERNS.md (pair-review.md section)
    - .planning/phases/06.1-bidirectional-llm-collaboration-auto-generation/06.1-CONTEXT.md (D-01, D-02, D-03, D-17)
  </read_first>
  <action>
Fully rewrite commands/pair-review.md. The new content must include:

FRONTMATTER (YAML between triple-dash fences):
- description: "Open a PR review workspace with bidirectional collaboration"
- argument-hint: "<pr-url-or-number> | --local <base> <head> [--dry]"
- allowed-tools: comma-separated list of ALL 12 MCP tools. The exact list per D-17:
  mcp__git-review-plugin__start_review, mcp__git-review-plugin__set_pr_summary, mcp__git-review-plugin__set_walkthrough, mcp__git-review-plugin__run_self_review, mcp__git-review-plugin__list_files, mcp__git-review-plugin__get_hunk, mcp__git-review-plugin__reply_in_thread, mcp__git-review-plugin__draft_comment, mcp__git-review-plugin__resolve_thread, mcp__git-review-plugin__submit_review, mcp__git-review-plugin__await_user_request, mcp__git-review-plugin__respond_chat

BODY must contain these sections in order:

1. SOURCE ARGUMENT PARSING: Preserve the 3 shapes (github URL, github number, local base/head). Tell Claude to strip "--dry" from $ARGUMENTS before building the source.

2. STARTUP SEQUENCE (D-01, D-02, D-03):
   Step 1: Parse $ARGUMENTS for --dry flag
   Step 2: Call start_review with source arg
   Step 3: Share start_review summary with user verbatim
   Step 4: Unless --dry: call set_pr_summary, then call set_walkthrough
   Step 5: Enter LISTEN LOOP

3. LISTEN LOOP (D-04, D-06):
   Step 6: Call await_user_request (blocks until browser POST or ~5 min timeout)
   Step 7: Process returned payload by type:
   - "no_request" -> immediately call await_user_request again
   - "chat" -> read payload.message, formulate response, call respond_chat, then loop
   - "inline_comment" -> read payload (lineId, message, threadId), call reply_in_thread, optionally call draft_comment, then loop
   - "run_self_review" -> call run_self_review, then loop
   - "regenerate_summary" -> call set_pr_summary, then loop
   - "regenerate_walkthrough" -> call set_walkthrough, then loop
   Step 8: After ANY type, call await_user_request again

4. CRITICAL LOOP DISCIPLINE: Explicit imperative statement that Claude MUST continue calling await_user_request indefinitely. NEVER decide the loop is "done." The loop only ends when the MCP connection drops.

5. User input: $ARGUMENTS (at the end)

Use the exact wording from RESEARCH.md Pattern 5 and PATTERNS.md pair-review.md section as the template. The loop discipline warning must be emphatic and unambiguous.
  </action>
  <verify>
    <automated>cd /Users/connorbarr/dev/personal/git-review-plugin && grep -c 'await_user_request' commands/pair-review.md && grep -c 'respond_chat' commands/pair-review.md && grep -c 'dry' commands/pair-review.md</automated>
  </verify>
  <acceptance_criteria>
    - grep -c 'allowed-tools:' commands/pair-review.md returns 1
    - grep 'allowed-tools:' commands/pair-review.md | grep -c 'await_user_request' returns 1
    - grep 'allowed-tools:' commands/pair-review.md | grep -c 'respond_chat' returns 1
    - grep -c 'await_user_request' commands/pair-review.md returns >= 5 (allowed-tools + loop references)
    - grep -c 'respond_chat' commands/pair-review.md returns >= 2 (allowed-tools + loop instruction)
    - grep -c 'set_pr_summary' commands/pair-review.md returns >= 2 (allowed-tools + startup)
    - grep -c 'set_walkthrough' commands/pair-review.md returns >= 2 (allowed-tools + startup)
    - grep -c 'dry' commands/pair-review.md returns >= 3 (argument-hint + parse instruction + skip condition)
    - grep -c 'LISTEN LOOP' commands/pair-review.md returns >= 1
    - grep -c 'LOOP DISCIPLINE' commands/pair-review.md returns >= 1
    - grep -c 'no_request' commands/pair-review.md returns >= 1
    - grep -c 'run_self_review' commands/pair-review.md returns >= 2 (allowed-tools + loop handler)
    - grep 'allowed-tools:' commands/pair-review.md | tr ',' '\n' | wc -l returns 12
  </acceptance_criteria>
  <done>Slash command fully rewritten with auto-generation startup sequence, indefinite listen loop protocol, --dry flag support, and all 12 tools in allowed-tools. Loop discipline is explicitly and emphatically stated.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
The complete Phase 06.1 bidirectional collaboration system:
- Reverse communication channel (await_user_request MCP tool + POST /api/user-request + request queue)
- Chat panel (320px right panel, open by default, with message list, send input, queue indicator, Run review button)
- Inline comments (gutter + icon on diff line hover, InlineComposer with @claude detection)
- Slash command rewrite (auto-generation + listen loop + --dry)
  </what-built>
  <how-to-verify>
Preparation:
1. Open a terminal in the project directory
2. Run claude with plugin-dir ./ to start Claude Code with the plugin loaded
3. Run /reload-plugins to pick up the rewritten slash command

Test 1 - Auto-generation flow:
1. Run /pair-review with a real PR url or number (use a small PR)
2. Verify: browser opens to the review workspace
3. Verify: PR summary appears in the SummaryDrawer (auto-generated, no manual prompting)
4. Verify: Walkthrough appears in the WalkthroughBanner (auto-generated)
5. Verify: Chat panel is visible on the right side (320px, open by default)
6. Verify: Chat panel shows Listening status after generation completes

Test 2 - Chat round-trip:
1. Type "What is this PR about?" in the chat panel textarea
2. Press Enter
3. Verify: Your message appears immediately in the chat panel
4. Verify: Claude responds in the chat panel (may take a few seconds)
5. Verify: Claude's response relates to the PR

Test 3 - Inline comment (user note):
1. Hover over a diff line -- verify a blue + icon appears in the gutter
2. Click the + icon -- verify an InlineComposer appears below the line
3. Type "This needs a comment" (no @claude)
4. Click "Add comment" -- verify a thread appears below the line with your comment
5. Verify: No LLM response (this is a user-note, not an @claude tag)

Test 4 - Inline comment (@claude tag):
1. Hover over a different diff line, click the + icon
2. Type "What does this function do? @claude"
3. Verify: "@claude will respond" chip appears
4. Verify: Button label changes to "Ask Claude"
5. Click "Ask Claude" -- verify your comment appears as a thread
6. Verify: Claude responds in the thread (via reply_in_thread)

Test 5 - --dry mode:
1. In a new Claude Code session: /pair-review --dry with the same PR
2. Verify: browser opens with the diff
3. Verify: No auto-generated summary or walkthrough
4. Verify: Chat panel is visible and shows Listening
5. Verify: You can still send chat messages and get responses

Test 6 - Browser refresh persistence:
1. During an active session with some chat messages
2. Refresh the browser (Cmd+R)
3. Verify: Chat messages are preserved after refresh
4. Verify: Any inline comment threads are preserved
  </how-to-verify>
  <resume-signal>Type "approved" if all tests pass, or describe any issues found</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| slash command prompt to Claude behavior | The command prompt must reliably produce correct tool-call sequences |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06.1-14 | Tampering | Slash command loop exit | mitigate | Explicit CRITICAL LOOP DISCIPLINE section with imperative language; no_request returns immediately loop; user can re-invoke /pair-review if loop exits |
| T-06.1-15 | Spoofing | --dry flag injection | accept | $ARGUMENTS is user-controlled input in the users own terminal session; no security boundary between user and their own Claude Code session |
</threat_model>

<verification>
- Slash command file contains all 12 tools in allowed-tools
- --dry flag parsing instructions present
- Listen loop protocol is explicit and imperative
- Human-verify checkpoint confirms end-to-end round-trip
</verification>

<success_criteria>
The full Phase 06.1 bidirectional collaboration system works end-to-end. User runs /pair-review, gets auto-generated summary + walkthrough, can chat with Claude in the panel, can leave inline comments with @claude tagging, and Claude stays in the listen loop indefinitely. --dry mode skips generation. Chat and threads persist across browser refresh.
</success_criteria>

<output>
After completion, create .planning/phases/06.1-bidirectional-llm-collaboration-auto-generation/06.1-05-SUMMARY.md
</output>
