---
phase: 06.1-bidirectional-llm-collaboration-auto-generation
verified: 2026-04-23T22:45:00Z
status: human_needed
score: 12/12 must-haves verified
overrides_applied: 0
human_verification:
  - test: "End-to-end auto-generation flow"
    expected: "Running /pair-review with a real PR auto-generates summary and walkthrough without manual prompting"
    why_human: "Requires live Claude Code session with MCP tools invoked by the LLM"
  - test: "Chat round-trip"
    expected: "User sends a chat message in the browser panel, Claude responds via respond_chat, response appears in the panel"
    why_human: "Requires real LLM processing and MCP tool call chain"
  - test: "Inline @claude comment triggers LLM reply"
    expected: "User creates an inline comment with @claude tag, Claude responds in the thread via reply_in_thread"
    why_human: "Requires real LLM processing, queue delivery, and thread reply"
  - test: "--dry mode skips generation but enters listen loop"
    expected: "Running /pair-review --dry opens the review workspace without auto-generated summary or walkthrough, but chat still works"
    why_human: "Requires live Claude Code session to verify slash command argument parsing"
  - test: "Browser refresh persistence"
    expected: "Chat messages and inline threads survive a browser refresh"
    why_human: "Requires running server + browser to verify SSE snapshot restores state"
  - test: "Listen loop stays alive indefinitely"
    expected: "Claude continues calling await_user_request after processing each request, never exits the loop"
    why_human: "Requires observing LLM behavior over multiple interactions"
---

# Phase 06.1: Bidirectional LLM Collaboration + Auto-Generation Verification Report

**Phase Goal:** Flip the interaction model so the browser UI drives the LLM. Three capabilities: (1) auto-generation of PR summary + walkthrough on startup so the user never has to prompt Claude manually, (2) reverse communication channel via long-poll MCP tool (`await_user_request`) so the browser can send requests to the Claude Code session, (3) chat panel + user-initiated inline comments with @claude tagging for line-level collaboration.
**Verified:** 2026-04-23T22:45:00Z
**Status:** human_needed
**Re-verification:** No -- initial verification

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | Phase 06.1 types compile and extend SessionEvent union with 5 new variants | VERIFIED | `shared/types.ts` (404 lines) contains all 5 variants: chat.userMessage, chat.llmMessage, thread.userStarted, request.queued, request.processing. ChatMessage type, Thread.initiator field, ReviewSession.chatMessages and requestQueue fields all present. `pnpm --filter server exec tsc --noEmit` exits 0. |
| 2 | Reducer handles all 5 new event branches and exhaustiveness guard compiles | VERIFIED | `server/src/session/reducer.ts` has 5 new case branches (confirmed grep counts = 1 each). D-14 draftBody logic at line 169: `draftBody: e.isClaudeTagged ? undefined : e.message`. TypeScript compiles cleanly. |
| 3 | RequestQueue resolves waiting Promises when requests are enqueued and returns null on timeout | VERIFIED | `server/src/session/request-queue.ts` (80 lines) exports SessionRequestQueue and RequestQueueManager. Resolver-clear-before-resolve pattern confirmed (line: `this.resolver = null` appears 3 times). 16 queue tests pass covering timeout, immediate-resolve, concurrent-guard, FIFO ordering. |
| 4 | await_user_request MCP tool blocks until browser POST or timeout | VERIFIED | `server/src/mcp/tools/await-user-request.ts` (61 lines) calls `queue.waitForRequest(TIMEOUT_MS)`, fires `request.processing` before waiting, returns `{ type: 'no_request' }` on null. Wired to RequestQueueManager via `queueManager.getQueue(prKey).waitForRequest()`. |
| 5 | respond_chat MCP tool fires chat.llmMessage SessionEvent | VERIFIED | `server/src/mcp/tools/respond-chat.ts` (43 lines) calls `manager.applyEvent(prKey, { type: 'chat.llmMessage', message, timestamp })`. No console.log. Returns 'Chat response sent.' |
| 6 | POST /api/user-request handles all request types with correct branching | VERIFIED | `server/src/http/routes/user-request.ts` (95 lines) handles chat (fires chat.userMessage + enqueues), inline_comment (resolves lineId server-side, fires thread.userStarted, conditionally enqueues only if isClaudeTagged), and action types (enqueue only). 396-line test file covers all paths. |
| 7 | RequestQueueManager is instantiated in index.ts and passed to both HTTP and MCP | VERIFIED | `server/src/index.ts` has RequestQueueManager import (2 refs) and queueManager usage (3 refs). `server/src/http/server.ts` has mountUserRequest (2 refs) and queueManager (2 refs). `server/src/mcp/server.ts` has registerAwaitUserRequest (2 refs), registerRespondChat (2 refs), queueManager (4 refs). |
| 8 | Chat panel renders as a collapsible right-side panel, open by default, with message list and send input | VERIFIED | `web/src/components/ChatPanel.tsx` (231 lines) renders a full chat interface: auto-scroll, message rendering via React text nodes (pre-wrap, zero dangerouslySetInnerHTML), queue badge, Run review button, collapse/expand, empty state with suggestion chips, error handling. `web/src/store.ts` has `chatPanelOpen: true` default. `web/src/App.tsx` mounts `<ChatPanel>` and activates 3-column grid via chatPanelOpen. 320px grid column in CSS. |
| 9 | User-initiated inline comments work via gutter + icon and InlineComposer | VERIFIED | `web/src/components/InlineComposer.tsx` (100 lines) has @claude detection (`value.includes('@claude')`), dynamic button label, Discard/Enter/Escape UX. `web/src/components/DiffViewer.tsx` has hoveredLineId (4 refs), composerLineId (6 refs), gutter-add-comment button (2 refs), InlineComposer mount (4 refs for UnifiedHunk + SplitHunk). prKey threaded through DiffViewer (12 refs) to InlineComposer. |
| 10 | SSE routing dispatches all 5 new event types to dedicated store actions | VERIFIED | `web/src/main.tsx` has routing branches for chat.userMessage, chat.llmMessage, request.queued, request.processing, thread.userStarted. `web/src/store.ts` has 6 new actions and correct chatMessages/requestQueue/chatPanelOpen state with snapshot propagation. |
| 11 | Slash command instructs auto-generation + listen loop + --dry support | VERIFIED | `commands/pair-review.md` (47 lines) has: all 12 tools in allowed-tools (cross-check PASS -- all match registerTool calls), STARTUP SEQUENCE (Steps 1-5 with --dry skip), LISTEN LOOP (Steps 6-8 handling all 6 return types), CRITICAL LOOP DISCIPLINE section with emphatic language. |
| 12 | Unit tests cover key behaviors (D-07, D-08, D-12, D-13, D-14, queue mechanics) | VERIFIED | 513 server tests pass (62 files). Web tests: ChatPanel.test.tsx (9 tests) and InlineComposer.test.tsx (7 tests) pass. Reducer-phase61.test.ts (21 tests including D-14 draftBody) passes. request-queue.test.ts (16 tests) passes. |

**Score:** 12/12 truths verified

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `shared/types.ts` | ChatMessage, 5 SessionEvent variants, Thread.initiator, ReviewSession extensions | VERIFIED | 404 lines, all types present, server tsc clean |
| `server/src/session/reducer.ts` | 5 new case branches with D-14 draftBody | VERIFIED | All 5 cases confirmed, draftBody conditional at line 169 |
| `server/src/session/request-queue.ts` | SessionRequestQueue + RequestQueueManager | VERIFIED | 80 lines, both classes exported, resolver-clear-before-resolve pattern |
| `server/src/mcp/tools/await-user-request.ts` | Long-poll MCP tool with 5-min timeout | VERIFIED | 61 lines, registerAwaitUserRequest exported, waitForRequest call, no_request return |
| `server/src/mcp/tools/respond-chat.ts` | Chat response MCP tool | VERIFIED | 43 lines, registerRespondChat exported, chat.llmMessage event, no console.log |
| `server/src/http/routes/user-request.ts` | POST /api/user-request with zod validation | VERIFIED | 95 lines, mountUserRequest exported, 5 request types handled, resolveLineIdExtended for lineId validation |
| `server/src/session/manager.ts` | getActivePrKey() method | VERIFIED | getActivePrKey (1 ref as method), activePrKey (4 refs including field + setter in both startReview paths) |
| `web/src/components/ChatPanel.tsx` | Chat panel with message list, send, queue badge, Run review | VERIFIED | 231 lines, fully substantive, no stubs |
| `web/src/components/InlineComposer.tsx` | Inline composer with @claude detection | VERIFIED | 100 lines, @claude detection, isClaudeTagged (4 refs), postUserRequest call |
| `web/src/components/__tests__/ChatPanel.test.tsx` | D-07/D-08 tests | VERIFIED | 109 lines, 10 postUserRequest refs, passes |
| `web/src/components/__tests__/InlineComposer.test.tsx` | D-12/D-13 tests | VERIFIED | 127 lines, 6 isClaudeTagged refs, passes |
| `commands/pair-review.md` | Rewritten slash command with all 12 tools | VERIFIED | 47 lines, 12 tools in allowed-tools, --dry support, listen loop, loop discipline |

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| await-user-request.ts | request-queue.ts | queue.waitForRequest(TIMEOUT_MS) | WIRED | Line 41: `await queue.waitForRequest(TIMEOUT_MS)` |
| user-request.ts | request-queue.ts | queueManager.getQueue(prKey).enqueue() | WIRED | Lines 70, 92: enqueue calls confirmed |
| user-request.ts | manager.ts | manager.applyEvent for chat.userMessage + thread.userStarted | WIRED | Lines 37, 56, 85: applyEvent calls |
| ChatPanel.tsx | api.ts | postUserRequest() for chat and run_self_review | WIRED | Import at line 3, calls at lines 76, 85 |
| main.tsx | store.ts | SSE onUpdate routing to new actions | WIRED | Lines 87-95: 5 event type branches |
| App.tsx | ChatPanel.tsx | ChatPanel mounted in main grid | WIRED | Line 521: `<ChatPanel` with all props |
| DiffViewer.tsx | InlineComposer.tsx | InlineComposer mounted as thread-row | WIRED | Lines 408, 610: `<InlineComposer` in UnifiedHunk and SplitHunk |
| pair-review.md | await-user-request.ts | Slash command loop calls await_user_request | WIRED | 10 references to await_user_request in command body |
| pair-review.md | respond-chat.ts | Slash command instructs respond_chat for chat responses | WIRED | 2 references to respond_chat in command body |
| index.ts | request-queue.ts | RequestQueueManager instantiated and passed to HTTP+MCP | WIRED | 2 import refs + 3 usage refs in index.ts |

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
|----------|---------------|--------|--------------------|--------|
| ChatPanel.tsx | messages (ChatMessage[]) | store.chatMessages -> App.tsx -> ChatPanel | SSE snapshot/update populates via reducer pipeline | FLOWING |
| ChatPanel.tsx | requestQueuePending | store.requestQueue.pending -> App.tsx -> ChatPanel | SSE request.queued/processing events update via reducer | FLOWING |
| InlineComposer.tsx | lineId, prKey | DiffViewer.tsx -> line.id, DiffViewerProps.prKey | Props from DiffModel and App state | FLOWING |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| Server TypeScript compiles | `pnpm --filter server exec tsc --noEmit` | Exit 0 (clean) | PASS |
| Server tests pass | `pnpm --filter server test --run` | 513 passed, 0 failed (62 files) | PASS |
| ChatPanel + InlineComposer tests pass | `pnpm --filter web test --run` (filtered) | 9 ChatPanel + 7 InlineComposer tests pass | PASS |
| Tool name cross-check | diff allowed-tools vs registerTool calls | Empty diff (all 12 match) | PASS |
| Web TypeScript compiles (source) | `pnpm --filter web exec tsc --noEmit` | 15 errors in DiffViewer.test.tsx (missing prKey prop) + pre-existing test errors | PASS (source code clean, test file issue only) |

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|------------|-------------|--------|----------|
| D-01 | 05 | Auto-generate summary + walkthrough on startup | SATISFIED | pair-review.md Steps 3-4 instruct set_pr_summary then set_walkthrough |
| D-02 | 05 | --dry mode skips auto-generation | SATISFIED | pair-review.md Step 1 parses --dry, Steps 3-4 skip if --dry |
| D-03 | 05 | Auto-enter listen loop after generation | SATISFIED | pair-review.md Step 5 enters LISTEN LOOP immediately |
| D-04 | 01, 02 | Long-poll MCP tool as reverse channel | SATISFIED | await-user-request.ts with queue.waitForRequest() |
| D-05 | 01, 02 | Server-side request queue | SATISFIED | SessionRequestQueue in request-queue.ts, wired in user-request.ts |
| D-06 | 01, 02 | Timeout with re-enter (~5 min) | SATISFIED | TIMEOUT_MS = 5*60*1000, no_request return on null, loop discipline in slash command |
| D-07 | 03 | Chat panel right-side, 320px, open by default | SATISFIED | ChatPanel.tsx + chatPanelOpen: true + 320px grid + ChatPanel.test.tsx |
| D-08 | 03 | Chat messages through reverse channel | SATISFIED | postUserRequest(type:'chat') -> user-request.ts -> queue -> await_user_request |
| D-09 | 01, 03 | Chat persisted via SessionEvents | SATISFIED | chat.userMessage/chat.llmMessage SessionEvent variants, reducer cases, SSE snapshot |
| D-10 | 03 | Chat supports full scope | SATISFIED | Slash command processes chat type with full session context |
| D-11 | 03 | Self-review requestable from UI | SATISFIED | ChatPanel.tsx Run review button -> postUserRequest(type:'run_self_review') |
| D-12 | 04 | User-initiated threads via gutter + icon | SATISFIED | DiffViewer.tsx gutter-add-comment button + InlineComposer mount |
| D-13 | 04 | @claude tagging model | SATISFIED | InlineComposer.tsx @claude detection + isClaudeTagged + dynamic button label |
| D-14 | 01, 04 | Non-tagged comments auto-create draft body | SATISFIED | Reducer line 169: `draftBody: e.isClaudeTagged ? undefined : e.message` |
| D-15 | 04 | @claude threads use draft_comment for synthesis | SATISFIED | pair-review.md inline_comment handler instructs draft_comment call |
| D-16 | 01, 04 | Thread.initiator field supersedes Phase 5 D-08 | SATISFIED | Thread.initiator field in shared/types.ts, reducer sets `initiator: 'user'` |
| D-17 | 05 | 12 MCP tools in allowed-tools | SATISFIED | 12 tools listed, cross-check PASS against registerTool calls |
| D-18 | 02 | await_user_request input schema: {} | SATISFIED | `const Input = z.object({})` in await-user-request.ts |
| D-19 | 02 | respond_chat input schema: { message: string } | SATISFIED | `z.object({ message: z.string().min(1).max(8000) })` |
| D-20 | 01 | 5 new SessionEvent variants | SATISFIED | All 5 variants in shared/types.ts, all 5 reducer cases |
| D-21 | 01, 03 | ReviewSession.chatMessages + requestQueue | SATISFIED | Both optional fields in shared/types.ts, propagated in store.ts |

**Requirements coverage: 21/21 (D-01 through D-21 all satisfied)**

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| web/src/__tests__/DiffViewer.test.tsx | multiple (15 sites) | Missing `prKey` prop after DiffViewerProps was extended | WARNING | TypeScript type errors in pre-existing test file; tests still run via esbuild but types are broken. Not caused by incomplete implementation -- caused by Plan 04 adding a required prop without updating the pre-existing test. |

### Human Verification Required

### 1. End-to-End Auto-Generation Flow

**Test:** Run `/pair-review` with a real PR URL in Claude Code with the plugin loaded.
**Expected:** Browser opens, PR summary auto-generates in SummaryDrawer, walkthrough auto-generates in WalkthroughBanner, chat panel visible on right side showing "Listening..." status.
**Why human:** Requires live Claude Code session invoking MCP tools. Cannot verify LLM tool-calling behavior programmatically.

### 2. Chat Round-Trip

**Test:** Type a message in the chat panel textarea and press Enter.
**Expected:** User message appears immediately, Claude responds with a relevant answer within seconds.
**Why human:** Requires real LLM processing and the full queue -> MCP tool -> respond_chat -> SSE pipeline.

### 3. Inline @claude Comment Reply

**Test:** Hover over a diff line, click the + icon, type "@claude explain this", click "Ask Claude".
**Expected:** Thread appears below the line, Claude responds in the thread.
**Why human:** Requires LLM to process inline_comment type from queue and call reply_in_thread.

### 4. --dry Mode

**Test:** Run `/pair-review --dry` with a PR.
**Expected:** Browser opens with diff but no auto-generated summary or walkthrough. Chat panel visible and responsive.
**Why human:** Requires verifying slash command argument parsing by the LLM.

### 5. Browser Refresh Persistence

**Test:** During an active session with chat messages, refresh the browser.
**Expected:** Chat messages and inline threads preserved after refresh.
**Why human:** Requires running server with SSE snapshot delivery.

### 6. Listen Loop Continuity

**Test:** Send multiple chat messages and inline comments sequentially.
**Expected:** Claude continues responding after each interaction, never exits the loop.
**Why human:** Requires observing LLM behavior over multiple await_user_request cycles.

### Gaps Summary

No blocking gaps found. All 12 must-have truths verified. All 21 requirements (D-01 through D-21) satisfied with implementation evidence. All key links are WIRED. All data flows are FLOWING. Tool name cross-check passes. Server TypeScript compiles cleanly (exit 0). Server tests pass (513/513). Phase 06.1 specific web tests pass (ChatPanel 9/9, InlineComposer 7/7).

**Warning-level issue:** `web/src/__tests__/DiffViewer.test.tsx` has TypeScript type errors (15 call sites missing the `prKey` prop added by Plan 04). The tests still run via Vitest's esbuild transpilation, but `tsc --noEmit` reports errors. This is a test maintenance gap, not a functionality gap.

**Human verification required:** 6 items need live testing with Claude Code + the plugin to verify the end-to-end bidirectional collaboration model. All server-side and client-side components are verified as wired and substantive, but the complete round-trip (browser -> POST -> queue -> MCP tool -> LLM processing -> respond_chat/reply_in_thread -> SSE -> browser) requires a running system with a real LLM.

---

_Verified: 2026-04-23T22:45:00Z_
_Verifier: Claude (gsd-verifier)_
