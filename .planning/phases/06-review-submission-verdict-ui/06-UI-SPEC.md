---
phase: 6
slug: review-submission-verdict-ui
status: draft
shadcn_initialized: false
preset: none
created: 2026-04-22
---

# Phase 6 — UI Design Contract

> Visual and interaction contract for the submit modal, verdict picker, and pending-review detection UI.
> Generated by gsd-ui-researcher, verified by gsd-ui-checker.

---

## Design System

| Property | Value |
|----------|-------|
| Tool | none — hand-rolled CSS with CSS custom properties (established Phase 1–5) |
| Preset | not applicable |
| Component library | none (no Radix, no shadcn) |
| Icon library | inline SVG via `web/src/components/icons.tsx` (`Ic.*` components) |
| Font | "Inter Tight" (sans), "JetBrains Mono" (mono) — declared in `--sans` / `--mono` tokens |

**Source:** `web/src/index.css` — fully established design system. No new design system setup required for Phase 6.

---

## Spacing Scale

Declared values from established token system (all multiples of 4):

| Token | Value | Usage |
|-------|-------|-------|
| xs | 4px | Icon gaps, inline badge padding |
| sm | 8px | Button internal gap, pill padding |
| md | 16px | Modal section padding, TopBar horizontal padding |
| lg | 24px | Modal body padding, major section gaps |
| xl | 32px | Modal max-content padding |
| 2xl | 48px | Modal max-width breathing room |
| 3xl | 64px | Not used in Phase 6 |

**Source:** Established in prior phases via `web/src/index.css` pattern (e.g., `.topbar` 16px, `.rp-head` 14px/16px, `.findings-row` 8px/16px).

Exceptions for Phase 6:
- Modal overlay: `position: fixed; inset: 0` — full viewport coverage, no spacing token.
- Modal card max-width: 560px (matches design.html submit modal proportions — wider than `StaleDiffModal` 448px to accommodate verdict cards + threads list).
- Verdict cards: 12px vertical padding, full-width horizontal layout with 8px gap between cards.
- Stats strip: 12px 16px padding — matches `.findings-sidebar-header` convention.

---

## Typography

Established from `web/src/index.css` body declaration and existing component patterns. No new sizes introduced for Phase 6.

| Role | Size | Weight | Line Height |
|------|------|--------|-------------|
| Body | 13px | 400 (regular) | 1.5 |
| Label / UI | 12px | 500 (medium) | 1.2 |
| Heading (modal title) | 14px | 600 (semibold) | 1.2 |
| Mono / code / stats | 11px–12px | 400 (regular) | 1.45 |

**Source:** `body { font-size: 13px; line-height: 1.45 }`, `.rp-name { font-size: 14px; font-weight: 600 }`, `.thread-msg .body { font-size: 12.5px }`, `.severity-pill { font-size: 11px; font-weight: 500 }` — all from `index.css`.

Phase 6 additions follow these exact sizes. No new type ramp entries.

---

## Color

All values from established `:root` tokens in `web/src/index.css`. No new CSS custom properties introduced for Phase 6.

| Role | Value / Token | Usage |
|------|---------------|-------|
| Dominant (60%) | `--paper: #FBFAF7` | Modal card background, overlay surface |
| Secondary (30%) | `--paper-2: #F5F3ED` | Stats strip background, threads list rows, modal header |
| Accent (10%) | `--claude: #2F6F66` | Approve verdict card selected state, active selection ring |
| Destructive | `--block: #9B3B2E` / `--block-bg: #F3DFD8` | "Request Changes" verdict card selected state, blocker count in stats |

**Source:** CONTEXT.md D-06 ("CSS token palette `--paper`, `--claude`, `--ok`, `--warn`, `--block` covers verdict card colors"), `index.css` `:root` block.

Accent (`--claude`) reserved for:
- Approve verdict card: selected border (`2px solid var(--claude)`) + background (`var(--claude-2)`)
- Active verdict ring: `box-shadow: 0 0 0 2px var(--claude)` when Approve is selected

Verdict card color assignment (locked from CONTEXT.md D-06 + design.html reference):
- **Approve** → selected: `--claude` border, `--claude-2` background, `--claude` text
- **Request Changes** → selected: `--block` border, `--block-bg` background, `--block` text (carries blocker badge count)
- **Comment** → selected: `--ink` border, `--paper-2` background, `--ink-2` text

Signal-ratio warning state: stats strip counts turn `color: var(--warn)` and `background: var(--warn-bg)` when ratio is nit-heavy (>3 nits OR signal ratio <40%). Submit button label changes from "Post review" to "Post anyway".

Overlay backdrop: `background: rgba(0,0,0,0.55)` — matches `StaleDiffModal` Tailwind `bg-black/70` intent, adapted to hand-rolled CSS token style.

**Source:** `--warn: #B5791F`, `--warn-bg: #F6EED9`, `--block: #9B3B2E`, `--block-bg: #F3DFD8`, `.severity-pill--blocker`, `.severity-pill--major` — all from `index.css`.

---

## Component Inventory

New components for Phase 6 (to be created in `web/src/components/`):

### `SubmitModal.tsx`

Self-guarding: renders only when `state.pendingSubmission` is set. Not dismissible via Escape or backdrop click — verdict must be explicitly chosen (mirrors `StaleDiffModal` non-dismissible pattern, per D-03 early-submit gate friction requirement).

Structure (top to bottom):
1. **Modal backdrop** — `position: fixed; inset: 0; background: rgba(0,0,0,0.55); z-index: 50; display: grid; place-items: center`
2. **Modal card** — `background: var(--paper); border-radius: var(--radius-lg); max-width: 560px; width: calc(100% - 32px); box-shadow: 0 16px 48px rgba(0,0,0,0.12)`
3. **Modal header** — `padding: 16px 20px; border-bottom: 1px solid var(--line); display: flex; align-items: center; justify-content: space-between`
   - Title: "Post review" at 14px weight 600 using `--ink`
   - Close button: icon-only `×`, `padding: 4px 6px; border-radius: 4px; color: var(--ink-4)` — enabled only when walkthrough is complete (see D-03)
4. **Stats strip** — `padding: 12px 20px; background: var(--paper-2); border-bottom: 1px solid var(--line); display: flex; gap: 16px; flex-wrap: wrap`
   - Four stat pills: `{N} blocker`, `{N} major`, `{N} minor`, `{N} nit` — each `font-family: var(--mono); font-size: 11px`
   - When nit-heavy: nit count pill turns `color: var(--warn); background: var(--warn-bg)`; whole strip adds an inline warning label "Nit-heavy review" at `color: var(--warn); font-size: 12px`
   - Walkthrough progress: "Walkthrough: {N}/{M} steps" monospace pill alongside stat pills
5. **Verdict cards section** — `padding: 16px 20px; display: flex; flex-direction: column; gap: 8px`
   - Section label: "Verdict" at `font-size: 12px; font-weight: 600; color: var(--ink-3); margin-bottom: 4px`
   - Three verdict cards, each: `display: flex; align-items: center; gap: 12px; padding: 12px 14px; border: 1.5px solid var(--line); border-radius: var(--radius); cursor: pointer; background: var(--paper)`
   - Selected verdict card: border color + background per color contract above; `box-shadow: 0 0 0 2px {accent-color}`
   - Card contents: radio-style dot (16px circle) + label text (13px weight 500) + optional badge (blocker count, `--block-bg`/`--block` using `.severity-pill--blocker` pattern)
6. **Review body textarea** — `padding: 0 20px 12px`
   - Section label: "Review summary" at 12px weight 600 `--ink-3`
   - Textarea: `width: 100%; min-height: 120px; max-height: 240px; resize: vertical; font-family: var(--sans); font-size: 13px; color: var(--ink); background: var(--paper); border: 1px solid var(--line-2); border-radius: var(--radius); padding: 10px 12px; line-height: 1.5`
   - Focus: `border-color: var(--ink-3); outline: 2px solid var(--claude); outline-offset: -1px` (matches `.thread-draft-input:focus` pattern)
7. **Threads-to-post list** — `padding: 0 20px 12px; max-height: 200px; overflow-y: auto`
   - Section label: "Inline comments ({N})" at 12px weight 600 `--ink-3`
   - Per thread row: `display: flex; align-items: flex-start; gap: 10px; padding: 8px 0; border-bottom: 1px solid var(--line); font-size: 12px`
   - Thread row contents: severity badge (`.severity-pill--{level}`) + file path (mono 11px `--ink-4`) + first line of draft body (13px `--ink-2`, truncated to 2 lines)
   - Empty state: "No inline comments drafted — the review will post summary only." at `color: var(--ink-4); font-size: 12px; padding: 12px 0`
8. **Incomplete walkthrough warning** — shown only when walkthrough incomplete
   - `padding: 10px 20px; background: var(--warn-bg); border-top: 1px solid var(--warn); color: var(--warn); font-size: 12px; display: flex; align-items: center; gap: 8px`
   - Text: "Walkthrough incomplete ({N}/{M} steps visited). Type your verdict to confirm early submit:"
   - Retype input: `border: 1px solid var(--warn); border-radius: var(--radius-sm); padding: 6px 10px; font-size: 12px; color: var(--ink); background: var(--paper); width: 100%`
9. **Modal footer** — `padding: 14px 20px; border-top: 1px solid var(--line); display: flex; justify-content: flex-end; gap: 8px`
   - Cancel button: `.btn-sm` — `background: var(--paper); border: 1px solid var(--line); color: var(--ink-2)` — only visible when walkthrough complete (when incomplete, there is no cancel — user must retype or not submit)
   - Submit button: `.btn-sm.primary` (`background: var(--ink); color: var(--paper); font-weight: 500`) — label: "Post review"
   - When nit-heavy AND walkthrough complete: submit button label changes to "Post anyway"
   - Disabled state when: no verdict selected, OR walkthrough incomplete AND retype not matched

### `PendingReviewModal.tsx`

Shown at session start when a pending GitHub review is detected. Same overlay/card structure as `SubmitModal` but narrower (max-width: 440px). Three buttons: "Adopt comments", "Clear pending review", "Cancel". Not dismissible via backdrop.

- Adopt: `.btn-sm.primary`
- Clear: `.btn-sm.danger` (`color: var(--block)`)
- Cancel: `.btn-sm`

### TopBar changes (in `TopBar.tsx`)

Replace `onApprove` / `onRequestChanges` button stubs with a single **"Submit review"** button:
- Class: `.topbar .primary` — `background: var(--ink); color: var(--paper); padding: 6px 12px; border-radius: 5px; font-weight: 500`
- Opens `SubmitModal` on click
- When `submissionState === 'submitted'`: button becomes "Review posted ✓" using `--ok` green, non-interactive

Verdict picker in TopBar: a compact inline segment control (same `.exp-toggle` pattern) showing current verdict selection — updates reactively from modal state. Hidden until `pendingSubmission` is set. Located left of "Submit review" button.

### StageStepper changes

Activate "Submit" step (currently `disabled: true` with `opacity: 0.5`):
- When `submissionState === 'not_yet'` or absent: stage shows as `active` when walkthrough is done, `default` before
- When `submissionState === 'submitted'`: stage shows as `done` with `--claude` check icon
- Remove `disabled: true` flag and `tooltip: 'Submit available in Phase 6'`

---

## Copywriting Contract

| Element | Copy |
|---------|------|
| Primary CTA (submit button) | "Post review" |
| Primary CTA when nit-heavy | "Post anyway" |
| Primary CTA when submitted | "Review posted ✓" |
| Modal title | "Post review" |
| Verdict option: Approve | "Approve" |
| Verdict option: Request Changes | "Request changes" |
| Verdict option: Comment | "Comment only" |
| Stats strip warning label | "Nit-heavy review — consider consolidating minor feedback" |
| Review body section label | "Review summary" |
| Threads section label | "Inline comments ({N})" |
| Threads empty state | "No inline comments drafted — the review will post summary only." |
| Incomplete walkthrough warning | "Walkthrough incomplete ({N}/{M} steps visited). Type your verdict to confirm early submit:" |
| Incomplete retype placeholder | e.g. "Type \"request changes\" to confirm" |
| Pending review modal title | "Existing pending review detected" |
| Pending review modal body | "A pending review exists on this PR. Adopt its comments into this session, or clear it before proceeding." |
| Pending review adopt button | "Adopt comments" |
| Pending review clear button | "Clear pending review" |
| Error: submission failed | "Post failed — {error message}. Check your network and try again." |
| Error: Octokit auth | "GitHub authentication error. Run `gh auth login` and reload." |
| Submitting in-progress | "Posting review…" (submit button text while pending) |
| Local export CTA | "Export to file" (replaces "Post review" in local-branch mode) |
| Local export success | "Review exported to {path}" |

**Source:** CONTEXT.md D-01 (modal surface), D-02 (signal-ratio warning), D-03 (incomplete walkthrough retype), D-04 (editable review body), design.html visual reference for verdict labels.

---

## Interaction Contracts

### Keyboard shortcuts (wiring existing stubs from Phase 3 D-18)

| Key | Action |
|-----|--------|
| `v` | Open verdict picker (the inline TopBar segment) or cycle to next verdict if already selected — implementation discretion per CONTEXT.md "Claude's Discretion" |
| `s` | Open submit modal directly (same as clicking "Submit review" button) |
| `Escape` | Close submit modal ONLY when walkthrough is complete (dismissed = no action). When walkthrough is incomplete, Escape is suppressed. |

**Source:** CONTEXT.md "Claude's Discretion" section (exact `v` behavior deferred); REQUIREMENTS.md PLUG-04.

### Two-step submit flow states

| State | UI |
|-------|----|
| `not_yet` | "Submit review" button in TopBar, normal. Modal closed. |
| `pending_confirmation` | Modal open, verdict + body pre-filled from LLM draft. User edits, clicks "Post review". |
| `submitting` | Submit button shows "Posting review…", disabled. Spinner optional (simple opacity pulse on button text). |
| `submitted` | Modal auto-closes. TopBar button shows "Review posted ✓" (`--ok` green). Submit stage in StageStepper shows `done`. |
| `failed` | Error message appears above footer in modal (12px `--block` color). Button re-enables as "Try again". |

**Source:** CONTEXT.md D-05 (two-step flow), D-10 (submission state machine), D-16 (SessionEvent variants).

### Focus management

- When modal opens: focus moves to verdict card group (first card) or retype input if walkthrough incomplete
- When modal closes (submitted or cancelled): focus returns to "Submit review" TopBar button
- Tab order within modal: verdict cards → review body textarea → threads list (read-only, tab-skipped) → [retype input if visible] → Cancel → Post review

---

## Accessibility

| Element | Implementation |
|---------|---------------|
| Modal | `role="dialog"` `aria-modal="true"` `aria-labelledby="submit-modal-title"` |
| Verdict cards | `role="radio"` within `role="radiogroup"` aria-label="Verdict" |
| Stats strip warning | `role="alert"` when warning state activates |
| Pending review modal | `role="alertdialog"` (blocking, requires action) |
| Submit button disabled | `aria-disabled="true"` (not HTML `disabled` — keeps focusable for screen readers) |
| Incomplete walkthrough | `aria-live="polite"` on retype validation message |

**Source:** Established pattern from `StaleDiffModal.tsx` (`role="dialog"`, `aria-modal="true"`, `aria-labelledby`).

---

## Modal CSS Class Names

New CSS classes to add to `web/src/index.css` under a `/* Phase 6 — Submit Modal */` section:

```
.submit-modal-backdrop       — fixed overlay, z-index: 50
.submit-modal-card           — white card, max-width 560px
.submit-modal-header         — title row
.submit-modal-stats          — stats strip (paper-2 bg)
.submit-modal-stats--warn    — warning modifier (warn-bg bg)
.submit-modal-verdicts       — verdict cards container
.verdict-card                — individual verdict card
.verdict-card--selected      — selected state modifier
.verdict-card--approve       — green selected colors
.verdict-card--request       — red selected colors
.verdict-card--comment       — ink selected colors
.submit-modal-body           — review body textarea section
.submit-modal-threads        — threads list section
.submit-modal-warn           — incomplete walkthrough warning strip
.submit-modal-footer         — footer with Cancel + Post buttons

.pending-review-modal-card   — narrower card (max-width 440px) for pending review
```

All new classes follow the existing naming convention (BEM-lite, hyphen-separated, no component prefix).

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| shadcn official | none | not applicable — no shadcn |
| Third-party | none | not applicable |

No third-party component registries. All components are hand-rolled following the established `index.css` + TSX pattern.

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS

**Approval:** pending
