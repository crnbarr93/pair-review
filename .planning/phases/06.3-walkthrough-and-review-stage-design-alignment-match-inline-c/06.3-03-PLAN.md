---
phase: 06.3-walkthrough-and-review-stage-design-alignment
plan: 03
type: execute
wave: 2
depends_on: ["06.3-01", "06.3-02"]
files_modified:
  - web/src/components/WalkthroughStepList.tsx
  - web/src/components/FindingsSidebar.tsx
  - web/src/components/SubmitModal.tsx
autonomous: true
requirements: []
must_haves:
  truths:
    - "Walkthrough step cards show green checkmark circles for completed steps, numbered circles for pending"
    - "Active walkthrough step card shows VIEWING badge and Ask about this step button"
    - "Findings cards show dual severity+category badge layout (e.g., BLOCKER SECURITY)"
    - "Active finding card has left border + background highlight"
    - "Finding cards have sparkle icon on Ask button and file icon with path:line reference"
    - "SubmissionPanel shows recap stats table with severity badges, Claude verdict suggestion, and CTA button"
  artifacts:
    - path: "web/src/components/WalkthroughStepList.tsx"
      provides: "Pixel-matched walkthrough step cards with VIEWING badge and Ask button"
      exports: ["WalkthroughStepList"]
    - path: "web/src/components/FindingsSidebar.tsx"
      provides: "Pixel-matched findings cards with dual badges, active highlighting, validity-aware dimming"
      exports: ["FindingsSidebar"]
    - path: "web/src/components/SubmitModal.tsx"
      provides: "Restyled SubmissionPanel with recap table, verdict card, CTA"
      exports: ["SubmissionPanel", "SubmitModal"]
  key_links:
    - from: "web/src/components/WalkthroughStepList.tsx"
      to: "web/src/api.ts"
      via: "postUserRequest for Ask about this step"
      pattern: "postUserRequest"
    - from: "web/src/components/FindingsSidebar.tsx"
      to: "shared/types.ts"
      via: "ResolvedFinding.validity field for dimming invalid findings"
      pattern: "validity"
---

<objective>
Pixel-match the WalkthroughStepList, FindingsSidebar, and SubmissionPanel right-panel components to the design.html mockup.

Purpose: D-03 through D-05 require walkthrough step card styling with VIEWING badge and Ask button. D-11 and D-12 require findings card restyling with dual badges and active highlighting. D-22 requires the SubmissionPanel recap table and verdict card.

Output: Three restyled right-panel components matching the design.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06.3-walkthrough-and-review-stage-design-alignment-match-inline-c/06.3-CONTEXT.md

<interfaces>
<!-- Key types the executor needs. -->

From shared/types.ts:
```typescript
export interface WalkthroughStep {
  stepNum: number;
  hunkId: string;
  commentary: string;
  status: WalkthroughStepStatus; // 'pending' | 'visited' | 'skipped'
}

export interface Walkthrough {
  steps: WalkthroughStep[];
  cursor: number;
  showAll: boolean;
  generatedAt: string;
}

export interface ResolvedFinding {
  id: string;
  category: ChecklistCategory;
  checklistItemId: string;
  severity: Severity;
  lineId: string;
  path: string;
  line: number;
  side: LineSide;
  title: string;
  rationale: string;
  validity?: 'valid' | 'invalid';  // Added by Plan 01
}

export type Verdict = 'request_changes' | 'comment' | 'approve';
```

From web/src/api.ts:
```typescript
export async function postUserRequest(prKey: string, request: { type: string; payload?: object }): Promise<void>;
export async function postSessionEvent(prKey: string, event: SessionEvent): Promise<void>;
```

From web/src/components/FindingsSidebar.tsx (current props):
```typescript
interface FindingsSidebarProps {
  selfReview: SelfReview | null | undefined;
  activeCategory: ChecklistCategory | null;
  onCategoryClick: (cat: ChecklistCategory | null) => void;
  onFindingClick: (lineId: string) => void;
  prKey?: string;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restyle WalkthroughStepList with design-matched cards, VIEWING badge, and Ask button</name>
  <files>web/src/components/WalkthroughStepList.tsx</files>
  <read_first>
    - web/src/components/WalkthroughStepList.tsx (current implementation — card structure, props, StepBadge component)
    - web/src/api.ts (postUserRequest for Ask button)
    - design.html (serve locally: `npx serve /Users/connorbarr/dev/personal/git-review-plugin -p 8765` then navigate to http://localhost:8765/design.html, click step 2 "Walkthrough" to see the step cards in the right panel)
  </read_first>
  <action>
    Restyle `WalkthroughStepList` to pixel-match the design (per D-03, D-04, D-05).

    **Props change:** Add `prKey` prop for the Ask button:
    ```typescript
    interface WalkthroughStepListProps {
      walkthrough: Walkthrough;
      onStepClick: (cursor: number) => void;
      onStepComplete: (index: number) => void;
      onStepToggle: (index: number, status: 'visited' | 'pending') => void;
      onShowAllToggle?: (showAll: boolean) => void;
      prKey: string;  // NEW — for Ask button postUserRequest
    }
    ```

    **Step badge circles (D-03):**
    - Completed steps: green circle (`var(--ok)`) with white checkmark SVG inside. Size 28px.
    - Pending steps: numbered circle with neutral border (`var(--line-2)`), number in `var(--ink-3)`. Size 28px.
    - Active step (cursor position): use the same pending/visited badge but with an active ring.

    **Card layout (D-03, D-05):**
    - Each card: subtle border `1px solid var(--line-2)`, border-radius 8px, padding 12px 16px.
    - Card header: badge circle + step title (first sentence of commentary, truncated at 60 chars).
    - Card body: file path in muted monospace `var(--mono)`, font-size 11px, color `var(--ink-4)`.
    - Card spacing: 8px gap between cards.

    **Active card VIEWING badge (D-04):**
    - When `i === cursor`: add a "VIEWING" badge in the card header. Style: small pill, background `var(--claude)`, color white, font-size 10px, uppercase, letter-spacing 0.5px.
    - The active card also gets a slightly different background: `var(--paper-2)` or a subtle left border `2px solid var(--claude)`.

    **Ask about this step button (D-04):**
    - Only on the active/current step card.
    - Button text: "Ask about this step" with a sparkle SVG icon.
    - On click: call `postUserRequest(prKey, { type: 'chat', payload: { message: \`Tell me more about step ${cursor+1}: "${step.commentary.split('.')[0]}". File: ${filePath}\` } })`.
    - Import `postUserRequest` from `'../api'`.
    - Style: small button, border `1px solid var(--line-2)`, background `var(--paper)`, hover background `var(--paper-2)`.

    **CSS classes:** Use `wsl-` prefix (already established). Add new classes as needed: `wsl-viewing-badge`, `wsl-ask-btn`, update existing `wsl-badge--done` to use green circle.
  </action>
  <verify>
    <automated>cd /Users/connorbarr/dev/personal/git-review-plugin && npx tsc --noEmit --project web/tsconfig.json 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'VIEWING' web/src/components/WalkthroughStepList.tsx` returns at least 1
    - `grep -c 'Ask about this step' web/src/components/WalkthroughStepList.tsx` returns at least 1
    - `grep -c 'postUserRequest' web/src/components/WalkthroughStepList.tsx` returns at least 1
    - `grep -c 'prKey' web/src/components/WalkthroughStepList.tsx` returns at least 2 (prop + usage)
    - TypeScript compilation succeeds
  </acceptance_criteria>
  <done>WalkthroughStepList shows green checkmark circles for completed steps, numbered for pending, VIEWING badge on active, Ask about this step button on active card.</done>
</task>

<task type="auto">
  <name>Task 2: Restyle FindingsSidebar with dual badges, active highlighting, and validity-aware dimming</name>
  <files>web/src/components/FindingsSidebar.tsx</files>
  <read_first>
    - web/src/components/FindingsSidebar.tsx (current implementation — card structure, filter tabs, severity labels)
    - shared/types.ts (ResolvedFinding with new validity field from Plan 01)
    - web/src/api.ts (postSessionEvent for validity toggle, postUserRequest for Ask)
    - design.html (serve locally, click step 3 "Review" to see findings cards in the right panel — note dual badges, active card highlight, file icon)
  </read_first>
  <action>
    Restyle `FindingsSidebar` to pixel-match the design (per D-11, D-12) and add validity-aware dimming (per D-14).

    **Props change:** Add `activeFindingId` and `onValidityToggle`:
    ```typescript
    interface FindingsSidebarProps {
      selfReview: SelfReview | null | undefined;
      activeCategory: ChecklistCategory | null;
      onCategoryClick: (cat: ChecklistCategory | null) => void;
      onFindingClick: (lineId: string) => void;
      prKey?: string;
      activeFindingId?: string | null;  // NEW — which finding card is expanded/active
    }
    ```

    **Dual badge layout (D-11):**
    - Replace single severity badge with two side-by-side badges: severity + category.
    - Example: `[BLOCKER] [SECURITY]` or `[WARNING] [PERFORMANCE]`.
    - Severity badge colors: blocker = `var(--block)` bg, major/minor = `var(--warn)` bg, nit = `var(--paper-3)` bg.
    - Category badge: neutral background `var(--paper-3)`, text `var(--ink-3)`.
    - Both badges: font-size 10px, uppercase, letter-spacing 0.5px, border-radius 4px, padding 2px 6px.

    **Active card highlighting (D-12):**
    - When `activeFindingId === f.id`: card gets a left border `3px solid` in severity color + subtle background tint.
    - CSS class: `findings-card--active`.

    **Sparkle icon on Ask button (D-11):**
    - Replace plain "Ask" text with sparkle SVG + "Ask" (same sparkle icon used elsewhere in the codebase).
    - CSS class: `findings-card-ask` (already exists, restyle).

    **File icon + path:line reference (D-11):**
    - File icon SVG before the path:line text (already exists, keep but ensure proper sizing: 12px).
    - Path:line in monospace `var(--mono)`, font-size 11px, color `var(--ink-4)`.

    **Validity-aware dimming (D-14):**
    - If `f.validity === 'invalid'`: card gets reduced opacity (0.5), strikethrough on the title text.
    - CSS class: `findings-card--invalid`.
    - Update the counts in the header: show "X open, Y dismissed" instead of just "X open".
    - Dismissed count = findings with `validity === 'invalid'`.
    - Open count = findings without `validity === 'invalid'`.

    **Finding validity toggle (D-13):**
    - On each finding card, add a small toggle area: "IS THIS FINDING VALID?" with two buttons: "Valid" (checkmark) and "Invalid" (X).
    - Clicking "Invalid": call `postSessionEvent(prKey, { type: 'finding.validitySet', findingId: f.id, validity: 'invalid' })`.
    - Clicking "Valid": call `postSessionEvent(prKey, { type: 'finding.validitySet', findingId: f.id, validity: 'valid' })`.
    - Import `postSessionEvent` from `'../api'`.
    - Show current validity state: if valid, checkmark highlighted green; if invalid, X highlighted red; if unset, both neutral.
    - CSS class: `findings-validity`, `findings-validity-btn`, `findings-validity-btn--active`.

    **Filter update:** The "Open" filter tab should exclude findings with `validity === 'invalid'`. The "All" tab shows everything (invalid ones dimmed).
  </action>
  <verify>
    <automated>cd /Users/connorbarr/dev/personal/git-review-plugin && npx tsc --noEmit --project web/tsconfig.json 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'findings-card--active' web/src/components/FindingsSidebar.tsx` returns at least 1
    - `grep -c 'findings-card--invalid' web/src/components/FindingsSidebar.tsx` returns at least 1
    - `grep -c 'validity' web/src/components/FindingsSidebar.tsx` returns at least 3 (dimming, toggle, counts)
    - `grep -c 'finding.validitySet' web/src/components/FindingsSidebar.tsx` returns at least 1
    - `grep -c 'postSessionEvent' web/src/components/FindingsSidebar.tsx` returns at least 1
    - `grep -c 'VALID' web/src/components/FindingsSidebar.tsx` returns at least 1 (validity toggle label)
    - TypeScript compilation succeeds
  </acceptance_criteria>
  <done>FindingsSidebar shows dual severity+category badges, active card highlighting, sparkle Ask button, validity toggle with dismiss+dim behavior, and updated open/dismissed counts.</done>
</task>

<task type="auto">
  <name>Task 3: Restyle SubmissionPanel to match design recap table and verdict card</name>
  <files>web/src/components/SubmitModal.tsx</files>
  <read_first>
    - web/src/components/SubmitModal.tsx (current SubmissionPanel component — recap stats, suggestion card, CTA)
    - web/src/store.ts (useAppStore for state access)
    - design.html (serve locally, click step 4 "Submission" to see the right panel content — recap stats table, Claude verdict suggestion card, CTA button)
  </read_first>
  <action>
    Restyle the `SubmissionPanel` component (exported from SubmitModal.tsx) to match the design (per D-22).

    **Recap stats table (D-22):**
    - Restructure the checklist section into a proper table/grid layout:
      - Row 1: "Stages completed" — value: "3 of 3 . Summary, Walkthrough, Review" (derive from actual completion state)
      - Row 2: "Findings" — value: severity badges inline (blocker count in red badge, warning count in amber badge, open count)
      - Row 3: "CI Checks" — value: pass/fail/pending badge (read from `state.ciStatus`)
      - Row 4: "Walkthrough progress" — value: "X of Y steps visited"
    - Each row: label on left in `var(--ink-3)` 12px, value on right in `var(--ink)` 12px.
    - Rows separated by subtle border-bottom `1px solid var(--line-2)`.
    - CSS classes: `subp-table`, `subp-table-row`, `subp-table-label`, `subp-table-value`.

    **CI Checks row:**
    - Read `ciStatus` from the store via `useAppStore()`. It's already available.
    - Display: green "Passed" badge if all checks pass, red "Failed" if any fail, yellow "Pending" if running.
    - If no CI data: show "No checks" in muted text.

    **Claude verdict suggestion card (D-22):**
    - Card with subtle background `var(--paper-2)`, border-radius 8px, padding 12px 16px.
    - Header: sparkle icon + "Claude suggests: [verdict]" in semi-bold.
    - "Why?" link/button that when clicked either:
      - Sends a chat message asking Claude to explain the verdict reasoning, OR
      - Toggles an inline expansion showing blocker rationale text.
    - Use the simpler approach: clicking "Why?" sends `postUserRequest(prKey, { type: 'chat', payload: { message: 'Why do you suggest this verdict? Explain your reasoning.' } })`.
    - CSS classes: `subp-verdict-card`, `subp-verdict-header`, `subp-verdict-why`.

    **CTA button (D-22):**
    - "Open submission dialog" button stays as is but add the sub-text: "Choose approve / comment / request changes".
    - Style: full-width, background `var(--claude)`, color white, border-radius 8px, padding 12px 16px.
    - CSS classes: keep existing `subp-cta`.

    **Do NOT modify** the `SubmitModal` or `SubmissionContent` components — only `SubmissionPanel`.
  </action>
  <verify>
    <automated>cd /Users/connorbarr/dev/personal/git-review-plugin && npx tsc --noEmit --project web/tsconfig.json 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'subp-table' web/src/components/SubmitModal.tsx` returns at least 2 (table class usages)
    - `grep -c 'ciStatus' web/src/components/SubmitModal.tsx` returns at least 1 (CI checks in recap)
    - `grep -c 'subp-verdict-card' web/src/components/SubmitModal.tsx` returns at least 1
    - `grep -c 'Why' web/src/components/SubmitModal.tsx` returns at least 1 (Why? link)
    - `grep -c 'SubmissionPanel' web/src/components/SubmitModal.tsx` returns at least 2 (function + export)
    - TypeScript compilation succeeds
  </acceptance_criteria>
  <done>SubmissionPanel shows recap stats table (stages, findings with severity badges, CI checks, walkthrough progress), Claude verdict suggestion card with Why? link, and styled CTA button.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser POST -> Server | Ask button sends chat via postUserRequest; validity toggle sends via postSessionEvent |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-63-06 | T (Tampering) | FindingsSidebar validity toggle | mitigate | postSessionEvent uses existing token auth middleware; reducer validates findingId exists |
| T-63-07 | I (Info Disclosure) | SubmissionPanel CI checks | accept | CI status already in session state visible to user; no new data exposure |
| T-63-08 | S (Spoofing) | WalkthroughStepList Ask button | mitigate | postUserRequest uses existing token auth; no new auth surface |
</threat_model>

<verification>
- `npx tsc --noEmit --project web/tsconfig.json` compiles cleanly
- All three components render correctly with their updated props
- FindingsSidebar shows validity toggle and dims invalid findings
- WalkthroughStepList shows VIEWING badge on active card and Ask button
- SubmissionPanel shows recap stats table with CI check status
</verification>

<success_criteria>
- WalkthroughStepList cards match design: green checkmarks, numbered circles, VIEWING badge, Ask button
- FindingsSidebar cards match design: dual badges, active highlighting, validity toggle with dimming
- SubmissionPanel matches design: recap table with severity badges and CI checks, verdict card, CTA
- All components compile cleanly with TypeScript
</success_criteria>

<output>
After completion, create `.planning/phases/06.3-walkthrough-and-review-stage-design-alignment-match-inline-c/06.3-03-SUMMARY.md`
</output>
