---
phase: 06.3-walkthrough-and-review-stage-design-alignment
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - shared/types.ts
  - server/src/session/reducer.ts
  - server/src/session/__tests__/reducer-phase63.test.ts
  - web/src/components/WalkthroughStepBanner.tsx
autonomous: true
requirements: []
must_haves:
  truths:
    - "ResolvedFinding type has an optional validity field of type 'valid' | 'invalid'"
    - "SessionEvent union includes a finding.validitySet variant"
    - "Reducer handles finding.validitySet by updating the finding's validity in selfReview.findings"
    - "Reducer test for finding.validitySet passes"
    - "WalkthroughStepBanner component renders step info above the diff"
  artifacts:
    - path: "shared/types.ts"
      provides: "finding.validitySet event variant and ResolvedFinding.validity field"
      contains: "finding.validitySet"
    - path: "server/src/session/reducer.ts"
      provides: "finding.validitySet case branch"
      contains: "case 'finding.validitySet'"
    - path: "server/src/session/__tests__/reducer-phase63.test.ts"
      provides: "Tests for finding.validitySet reducer branch"
      contains: "finding.validitySet"
    - path: "web/src/components/WalkthroughStepBanner.tsx"
      provides: "Step info banner for walkthrough mode"
      exports: ["WalkthroughStepBanner"]
  key_links:
    - from: "server/src/session/reducer.ts"
      to: "shared/types.ts"
      via: "SessionEvent union import"
      pattern: "case 'finding.validitySet'"
---

<objective>
Add the finding validity infrastructure (types, reducer, test) and create the WalkthroughStepBanner component.

Purpose: D-15 and D-16 require a new SessionEvent variant and ResolvedFinding field for the validity toggle. D-01 and D-02 require a step info banner above the diff in Walkthrough mode. These are foundational for Plans 02-04.

Output: Extended types, reducer branch with test, new WalkthroughStepBanner component.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06.3-walkthrough-and-review-stage-design-alignment-match-inline-c/06.3-CONTEXT.md

<interfaces>
<!-- Key types and contracts the executor needs. -->

From shared/types.ts:
```typescript
export interface ResolvedFinding {
  id: string;
  category: ChecklistCategory;
  checklistItemId: string;
  severity: Severity;
  lineId: string;
  path: string;
  line: number;
  side: LineSide;
  title: string;
  rationale: string;
}

export type SessionEvent =
  // ... existing variants ...
  | { type: 'request.processing' };
  // ^ last variant — new ones go after this

export interface WalkthroughStep {
  stepNum: number;
  hunkId: string;
  commentary: string;
  status: WalkthroughStepStatus;
}

export interface Walkthrough {
  steps: WalkthroughStep[];
  cursor: number;
  showAll: boolean;
  generatedAt: string;
}
```

From server/src/session/reducer.ts:
```typescript
export function applyEvent(s: ReviewSession, e: SessionEvent): ReviewSession {
  switch (e.type) {
    // ... existing cases ...
    case 'request.processing':
      // ^ last case — new case goes before the default
    default: {
      const _never: never = e;
      throw new Error(`Unknown event type: ${JSON.stringify(_never)}`);
    }
  }
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add finding validity types + reducer branch + test</name>
  <files>shared/types.ts, server/src/session/reducer.ts, server/src/session/__tests__/reducer-phase63.test.ts</files>
  <read_first>
    - shared/types.ts (current ResolvedFinding interface at ~line 292, SessionEvent union at ~line 112)
    - server/src/session/reducer.ts (current case branches, pattern for new cases)
    - server/src/session/__tests__/reducer-phase61.test.ts (test pattern: baseSession factory, describe/it structure)
  </read_first>
  <behavior>
    - Test 1: applying finding.validitySet with validity='invalid' to a session with selfReview containing a matching finding sets that finding's validity to 'invalid'
    - Test 2: applying finding.validitySet with validity='valid' to a session with selfReview sets the finding's validity to 'valid'
    - Test 3: applying finding.validitySet with a non-existent findingId returns the session unchanged (no crash)
    - Test 4: applying finding.validitySet when selfReview is null/undefined returns the session unchanged
  </behavior>
  <action>
    1. In `shared/types.ts`:
       - Add `validity?: 'valid' | 'invalid'` field to the `ResolvedFinding` interface (per D-16), after the `rationale` field.
       - Add a new SessionEvent variant to the union (per D-15):
         `| { type: 'finding.validitySet'; findingId: string; validity: 'valid' | 'invalid' }`
         Place it after the last Phase 06.1 variant (`request.processing`) and before the closing semicolon. Add a comment: `// Phase 06.3 additions (D-15):`.

    2. In `server/src/session/reducer.ts`:
       - Add a new case branch `case 'finding.validitySet':` before the `default:` branch.
       - Implementation: if `s.selfReview` is null/undefined, return `s` unchanged. Otherwise, map over `s.selfReview.findings` and for the finding whose `id === e.findingId`, spread and set `validity: e.validity`. Return `{ ...s, selfReview: { ...s.selfReview, findings: updatedFindings } }`.
       - Add a comment above: `// Phase 06.3 additions (D-15) — finding validity toggle:`

    3. In `server/src/session/__tests__/reducer-phase63.test.ts`:
       - Create a new test file following the pattern from `reducer-phase61.test.ts`.
       - Use a `baseSession()` factory function (copy from reducer-phase61.test.ts).
       - Create a helper `baseSessionWithFindings()` that returns a session with `selfReview: { findings: [{ id: 'f1', ... }, { id: 'f2', ... }], coverage: {...}, verdict: 'request_changes', generatedAt: '...' }`.
       - Write tests for the 4 behaviors listed above.
  </action>
  <verify>
    <automated>cd /Users/connorbarr/dev/personal/git-review-plugin && npx vitest run server/src/session/__tests__/reducer-phase63.test.ts --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "validity" shared/types.ts` returns at least 3 (field definition + event type field + event variant type name)
    - `grep -c "finding.validitySet" shared/types.ts` returns 1
    - `grep -c "case 'finding.validitySet'" server/src/session/reducer.ts` returns 1
    - `grep -c "finding.validitySet" server/src/session/__tests__/reducer-phase63.test.ts` returns at least 4
    - vitest run for reducer-phase63.test.ts exits 0 with all tests passing
  </acceptance_criteria>
  <done>ResolvedFinding has validity field, SessionEvent has finding.validitySet variant, reducer handles it, 4 tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Create WalkthroughStepBanner component</name>
  <files>web/src/components/WalkthroughStepBanner.tsx</files>
  <read_first>
    - web/src/components/WalkthroughBanner.tsx (deprecated predecessor — see structure and props pattern)
    - web/src/components/WalkthroughStepList.tsx (current step card component — see how steps/cursor are accessed)
    - shared/types.ts (Walkthrough, WalkthroughStep types)
    - design.html (serve locally: `npx serve /Users/connorbarr/dev/personal/git-review-plugin -p 8765` then navigate to http://localhost:8765/design.html, click step 2 "Walkthrough" in the step nav bar to see the banner above the diff)
  </read_first>
  <action>
    Create `web/src/components/WalkthroughStepBanner.tsx` — a step info banner that renders above the diff in Walkthrough mode (per D-01, D-02).

    **Props interface:**
    ```typescript
    interface WalkthroughStepBannerProps {
      walkthrough: Walkthrough;
      onStepClick: (cursor: number) => void;
    }
    ```

    **Content to render (per D-01):**
    - Top line: "STEP {cursor+1} OF {steps.length} . {visitedCount}/{steps.length} reviewed" in small caps monospace
    - Step title: first sentence of `steps[cursor].commentary` in 14px semi-bold
    - Step description: remainder of commentary in 13px regular color `var(--ink-2)`
    - "Reviewed" badge: if current step status is 'visited', show a green checkmark badge "Reviewed" with `var(--ok)` color
    - Prev/Next navigation cards (per D-02):
      - "PREV" card shows previous step title (if cursor > 0), clicks fire `onStepClick(cursor - 1)`
      - "NEXT" card shows next step title (if cursor < steps.length - 1), clicks fire `onStepClick(cursor + 1)`
      - Cards should have subtle border, small font, and arrow indicators

    **Styling:**
    - Flat background `var(--paper-2)` — NOT the gradient shown in the design (per D-01 explicit instruction)
    - Border-bottom `1px solid var(--line-2)`
    - Padding `12px 20px`
    - Use CSS classes (not inline styles) prefixed with `wt-banner-` for all styling. Add the CSS to the bottom of the component file using a `<style>` approach OR (preferred) use Tailwind-like utility classes from the project's existing pattern. Check how other components handle CSS — if they use index.css, add a comment noting CSS will be added in Plan 03.

    **Important:** This component does NOT include the `<style>` block — CSS will be added in Plan 04's index.css task. Use className-based styling and document expected class names at the top of the file.
  </action>
  <verify>
    <automated>cd /Users/connorbarr/dev/personal/git-review-plugin && npx tsc --noEmit --project web/tsconfig.json 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "WalkthroughStepBanner" web/src/components/WalkthroughStepBanner.tsx` returns at least 2 (component name + export)
    - `grep -c "export function WalkthroughStepBanner" web/src/components/WalkthroughStepBanner.tsx` returns 1
    - `grep -c "STEP" web/src/components/WalkthroughStepBanner.tsx` returns at least 1
    - `grep -c "onStepClick" web/src/components/WalkthroughStepBanner.tsx` returns at least 2 (prop + usage)
    - `grep -c "PREV\|NEXT" web/src/components/WalkthroughStepBanner.tsx` returns at least 2
    - TypeScript compilation succeeds (exit 0)
  </acceptance_criteria>
  <done>WalkthroughStepBanner component exists with step info, reviewed badge, and prev/next navigation cards. TypeScript compiles cleanly.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser POST -> Server | finding.validitySet event sent from browser via postSessionEvent |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-63-01 | T (Tampering) | finding.validitySet event | mitigate | Reducer validates findingId exists in selfReview.findings before mutating; non-existent ID returns session unchanged |
| T-63-02 | I (Info Disclosure) | WalkthroughStepBanner | accept | Commentary is already in session state visible to the user; no new disclosure surface |
</threat_model>

<verification>
- `npx vitest run server/src/session/__tests__/reducer-phase63.test.ts` passes all tests
- `npx tsc --noEmit --project web/tsconfig.json` compiles cleanly
- `grep -c "finding.validitySet" shared/types.ts server/src/session/reducer.ts` shows 1 in each file
</verification>

<success_criteria>
- ResolvedFinding type extended with validity field
- SessionEvent union extended with finding.validitySet variant
- Reducer handles finding.validitySet correctly (tested with 4 cases)
- WalkthroughStepBanner component created with step info, badge, and prev/next nav
- TypeScript compiles cleanly across both server and web workspaces
</success_criteria>

<output>
After completion, create `.planning/phases/06.3-walkthrough-and-review-stage-design-alignment-match-inline-c/06.3-01-SUMMARY.md`
</output>
