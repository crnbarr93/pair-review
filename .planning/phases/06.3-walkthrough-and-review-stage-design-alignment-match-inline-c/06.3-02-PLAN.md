---
phase: 06.3-walkthrough-and-review-stage-design-alignment
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - web/src/components/ThreadCard.tsx
  - web/src/components/InlineComposer.tsx
autonomous: true
requirements: []
must_haves:
  truths:
    - "ThreadCard uses CSS classes instead of inline styles"
    - "ThreadCard shows severity-colored header bar with severity label, line number, and category"
    - "ThreadCard shows Claude/You avatar circles with proper styling"
    - "ThreadCard shows styled message bubbles with timestamps"
    - "ThreadCard has a Reply text input with Reply button at the bottom for unresolved threads"
    - "InlineComposer visual styling matches ThreadCard treatment"
  artifacts:
    - path: "web/src/components/ThreadCard.tsx"
      provides: "Restyled thread card with severity header, avatars, reply input"
      exports: ["ThreadCard"]
    - path: "web/src/components/InlineComposer.tsx"
      provides: "Restyled inline comment composer matching ThreadCard design"
      exports: ["InlineComposer"]
  key_links:
    - from: "web/src/components/ThreadCard.tsx"
      to: "web/src/api.ts"
      via: "postUserRequest for reply submissions"
      pattern: "postUserRequest"
    - from: "web/src/components/InlineComposer.tsx"
      to: "web/src/api.ts"
      via: "postUserRequest for comment submissions"
      pattern: "postUserRequest"
---

<objective>
Restyle ThreadCard and InlineComposer to match the design.html mockup with severity-colored headers, proper avatar circles, styled message bubbles, and reply input.

Purpose: D-06 through D-09 require a full visual restyle of the inline thread and comment surfaces. ThreadCard moves from inline styles to CSS classes, gains a severity header bar, proper avatar styling, and a reply input. InlineComposer aligns visually with ThreadCard.

Output: Restyled ThreadCard and InlineComposer components.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06.3-walkthrough-and-review-stage-design-alignment-match-inline-c/06.3-CONTEXT.md

<interfaces>
<!-- Key types the executor needs. -->

From shared/types.ts:
```typescript
export interface Thread {
  threadId: string;
  lineId: string;
  path: string;
  line: number;
  side: LineSide;
  preExisting: boolean;
  initiator?: 'llm' | 'user';
  turns: ThreadTurn[];
  draftBody?: string;
  resolved: boolean;
  createdAt: string;
}

export interface ThreadTurn {
  author: 'llm' | 'user';
  message: string;
  createdAt: string;
}
```

From web/src/components/ThreadCard.tsx (current props):
```typescript
interface ThreadCardProps {
  thread: Thread;
  onDraftChange: (threadId: string, body: string) => void;
  onCollapse: () => void;
}
```

From web/src/components/InlineComposer.tsx (current props):
```typescript
interface InlineComposerProps {
  lineId: string;
  lineNumber: number;
  prKey: string;
  onClose: () => void;
}
```

From web/src/api.ts:
```typescript
export async function postUserRequest(prKey: string, request: { type: string; payload?: object }): Promise<void>;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restyle ThreadCard with severity header, avatars, message bubbles, and reply input</name>
  <files>web/src/components/ThreadCard.tsx</files>
  <read_first>
    - web/src/components/ThreadCard.tsx (current implementation — all inline styles to replace)
    - web/src/api.ts (postUserRequest function signature for reply submission)
    - design.html (serve locally: `npx serve /Users/connorbarr/dev/personal/git-review-plugin -p 8765` then navigate to http://localhost:8765/design.html, click step 3 "Review" to see the inline thread cards in the diff area. Look for the severity-colored header, avatar circles, and reply input)
  </read_first>
  <action>
    Rewrite `ThreadCard` to match the design mockup (per D-06, D-07, D-08).

    **Props change:** Add optional props to support finding context:
    ```typescript
    interface ThreadCardProps {
      thread: Thread;
      finding?: ResolvedFinding;  // Associated finding (if thread was generated from a finding)
      onDraftChange: (threadId: string, body: string) => void;
      onCollapse: () => void;
      prKey: string;              // For reply submissions via postUserRequest
    }
    ```

    **Header bar (D-06):**
    - Severity-colored top bar: red for blocker, amber for major/minor, muted for nit. Color from `finding?.severity`, fallback to neutral if no finding.
    - Content: `[SEVERITY] Line {thread.line} . {finding?.category ?? ''}` on the left, close/collapse button on the right.
    - CSS class: `tc-header`, `tc-header--blocker`, `tc-header--major`, `tc-header--minor`, `tc-header--nit`, `tc-header--neutral` (when no finding).
    - Use CSS variables: `var(--block)` for blocker, `var(--warn)` for major/minor, `var(--ink-4)` for nit/neutral.

    **Avatar circles (D-06):**
    - Claude avatar: 24px circle, background `var(--claude)`, white "C" letter centered.
    - User avatar: 24px circle, background `var(--ink-4)`, white "Y" letter centered.
    - CSS class: `tc-avatar`, `tc-avatar--claude`, `tc-avatar--user`.

    **Message bubbles (D-06):**
    - Each turn renders as: avatar + name + timestamp + message body.
    - Name: "Claude" or "You" in 12px semi-bold.
    - Timestamp: HH:MM format, 11px mono `var(--ink-4)`.
    - Message: 13px, `line-height: 1.5`, `white-space: pre-wrap`.
    - CSS class: `tc-turn`, `tc-turn-meta`, `tc-turn-name`, `tc-turn-time`, `tc-turn-body`.
    - SECURITY: message content MUST remain as React text node `{turn.message}` — NEVER innerHTML.

    **Reply input (D-08):**
    - At bottom of unresolved threads: a text input with placeholder "Reply..." and a "Reply" button.
    - On submit: call `postUserRequest(prKey, { type: 'inline_comment', payload: { lineId: thread.lineId, message: value.trim(), isClaudeTagged: value.includes('@claude') } })`.
    - Import `postUserRequest` from `'../api'`.
    - If `@claude` is detected, show a small chip "Claude will respond" like InlineComposer does.
    - CSS class: `tc-reply`, `tc-reply-input`, `tc-reply-btn`.

    **Draft slot:** Keep existing draft textarea behavior but use CSS classes:
    - `tc-draft-slot`, `tc-draft-input`.

    **ALL inline styles must be replaced with CSS classes (D-07).** Use the `tc-` prefix for all ThreadCard classes. The actual CSS rules will be added to `index.css` in Plan 04 Task 3.

    **For now, add a CSS block at the bottom of the file** using a style constant or embedded CSS-in-JS approach that can be moved to index.css later. Alternatively, simply use the class names and rely on the fact that Plan 04 will add the CSS rules. Either approach is fine as long as the component compiles.

    **Typing indicator:** Keep existing structure but use CSS class `tc-typing`.
  </action>
  <verify>
    <automated>cd /Users/connorbarr/dev/personal/git-review-plugin && npx tsc --noEmit --project web/tsconfig.json 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'style=' web/src/components/ThreadCard.tsx` returns 0 (all inline styles removed per D-07)
    - `grep -c 'tc-header' web/src/components/ThreadCard.tsx` returns at least 2 (class usage)
    - `grep -c 'tc-avatar' web/src/components/ThreadCard.tsx` returns at least 2
    - `grep -c 'tc-reply' web/src/components/ThreadCard.tsx` returns at least 2
    - `grep -c 'postUserRequest' web/src/components/ThreadCard.tsx` returns at least 1
    - `grep -c 'innerHTML' web/src/components/ThreadCard.tsx` returns 0 (SECURITY: no innerHTML for LLM content)
    - `grep -c 'dangerouslySetInnerHTML' web/src/components/ThreadCard.tsx` returns 0
    - TypeScript compilation succeeds
  </acceptance_criteria>
  <done>ThreadCard restyled with severity header bar, avatar circles, message bubbles, reply input, and zero inline styles. All CSS via class names.</done>
</task>

<task type="auto">
  <name>Task 2: Restyle InlineComposer to match ThreadCard visual treatment</name>
  <files>web/src/components/InlineComposer.tsx</files>
  <read_first>
    - web/src/components/InlineComposer.tsx (current implementation with inline styles)
    - web/src/components/ThreadCard.tsx (after Task 1 — see new class name patterns to match)
    - design.html (serve locally, click step 3 "Review" — look for the inline comment composer styling below diff lines)
  </read_first>
  <action>
    Restyle `InlineComposer` to visually match ThreadCard's treatment (per D-09).

    **Changes:**
    1. Replace ALL inline styles with CSS classes using `ic-` prefix:
       - `ic-panel` (was `thread-panel` with inline styles)
       - `ic-textarea` (was inline-styled textarea)
       - `ic-footer` (was `thread-reply-footer`)
       - `ic-claude-chip` (was `claude-chip`)
       - `ic-btn-discard`, `ic-btn-submit` (were `btn-sm` / `btn-sm primary`)

    2. Match the border treatment from ThreadCard:
       - Same left-border color scheme: `var(--claude)` for the panel left border
       - Same background: `var(--paper)`
       - Same border-radius and padding patterns

    3. Keep the existing @claude detection logic unchanged.
    4. Keep the existing keyboard handling (Enter to submit, Escape to close) unchanged.
    5. Keep all SECURITY comments intact.

    CSS rules for `ic-*` classes will be added in Plan 04 Task 3.
  </action>
  <verify>
    <automated>cd /Users/connorbarr/dev/personal/git-review-plugin && npx tsc --noEmit --project web/tsconfig.json 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'style=' web/src/components/InlineComposer.tsx` returns 0 (all inline styles removed per D-09)
    - `grep -c 'ic-' web/src/components/InlineComposer.tsx` returns at least 4 (class name usages)
    - `grep -c 'postUserRequest' web/src/components/InlineComposer.tsx` returns at least 1 (unchanged)
    - `grep -c '@claude' web/src/components/InlineComposer.tsx` returns at least 1 (detection preserved)
    - TypeScript compilation succeeds
  </acceptance_criteria>
  <done>InlineComposer restyled with CSS classes matching ThreadCard visual treatment. Zero inline styles. @claude detection and keyboard handling preserved.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User input -> postUserRequest | Reply text submitted via ThreadCard reply input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-63-03 | S (Spoofing) | ThreadCard reply input | mitigate | Reply goes through existing postUserRequest with token auth — same security as InlineComposer |
| T-63-04 | I (Info Disclosure) | ThreadCard | accept | Thread content is already visible to the user in the current UI; restyle does not change data exposure |
| T-63-05 | T (Tampering) | ThreadCard LLM content | mitigate | All turn.message and draftBody rendered as React text nodes only, never innerHTML — SECURITY comments preserved |
</threat_model>

<verification>
- `npx tsc --noEmit --project web/tsconfig.json` compiles cleanly
- `grep -c 'style=' web/src/components/ThreadCard.tsx web/src/components/InlineComposer.tsx` returns 0 for both files
- Both components maintain their existing props interface contracts (ThreadCard adds optional `finding` and `prKey`)
</verification>

<success_criteria>
- ThreadCard has severity-colored header, avatar circles, styled message bubbles, and reply input
- InlineComposer styling matches ThreadCard visual treatment
- Zero inline styles in either component (all CSS via class names)
- Security invariants preserved (no innerHTML for LLM content)
- TypeScript compiles cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/06.3-walkthrough-and-review-stage-design-alignment-match-inline-c/06.3-02-SUMMARY.md`
</output>
