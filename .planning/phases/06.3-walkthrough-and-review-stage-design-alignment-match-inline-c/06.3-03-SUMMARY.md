---
phase: 06.3-walkthrough-and-review-stage-design-alignment
plan: 03
subsystem: ui
tags: [react, typescript, walkthrough, findings, submission, design-alignment]

requires:
  - phase: 06.3-01
    provides: finding validity field on ResolvedFinding, finding.validitySet SessionEvent type
  - phase: 06.3-02
    provides: RightPanel component, stage navigation, active step routing

provides:
  - WalkthroughStepList with VIEWING badge, Ask button, green checkmarks for completed steps
  - FindingsSidebar with dual severity+category badges, active card highlighting, validity toggle with dimming
  - SubmissionPanel with recap stats table (stages/findings/CI/walkthrough), verdict card with Why? link

affects: [App.tsx, right-panel rendering, submission flow]

tech-stack:
  added: []
  patterns:
    - "Dual-badge pattern for severity+category on finding cards"
    - "Validity-aware rendering: opacity+strikethrough for dismissed findings"
    - "postSessionEvent for user-driven state mutations on finding validity"
    - "Active card left-border pattern using severity color variable"

key-files:
  created: []
  modified:
    - web/src/components/WalkthroughStepList.tsx
    - web/src/components/FindingsSidebar.tsx
    - web/src/components/SubmitModal.tsx
    - web/src/App.tsx

key-decisions:
  - "prKey prop added to WalkthroughStepList rather than reading from store, matching the prop-passing pattern used elsewhere"
  - "activeFindingId added as optional prop to FindingsSidebar to allow parent control of active card without adding new store state"
  - "SubmissionPanel recap table renamed from subp-checklist to subp-table with new CSS class pattern"
  - "Stage completion count derived from actual completion booleans rather than hardcoded 3 of 3"

requirements-completed: []

duration: 20min
completed: 2026-04-27
---

# Phase 06.3 Plan 03: Right-Panel Design Alignment (WalkthroughStepList, FindingsSidebar, SubmissionPanel) Summary

**Pixel-matched walkthrough step cards with VIEWING badge and Ask button, findings cards with dual severity+category badges and validity dimming, and SubmissionPanel recap table with CI status and Claude verdict card**

## Performance

- **Duration:** ~20 min
- **Started:** 2026-04-27T00:00:00Z
- **Completed:** 2026-04-27T00:20:00Z
- **Tasks:** 3 of 3
- **Files modified:** 4

## Accomplishments

- WalkthroughStepList: green checkmark circles for visited steps, numbered circles with active ring for pending, VIEWING badge on active card, "Ask about this step" button with sparkle icon calling postUserRequest
- FindingsSidebar: dual badge layout (BLOCKER/WARNING + category), active card left-border in severity color, validity toggle (IS THIS FINDING VALID?) with Valid/Invalid buttons using postSessionEvent, opacity+strikethrough dimming for invalid findings, open/dismissed count in subtitle
- SubmissionPanel: four-row recap table (stages completed, findings with inline badges, CI checks from store, walkthrough progress), Claude verdict suggestion card with Why? button posting chat request, styled CTA button

## Task Commits

Each task was committed atomically:

1. **Task 1: Restyle WalkthroughStepList with VIEWING badge and Ask button** - `b943b5c` (feat)
2. **Task 2: Restyle FindingsSidebar with dual badges, active highlighting, validity toggle** - `4facd9d` (feat)
3. **Task 3: Restyle SubmissionPanel with recap table and verdict card** - `2f18fb4` (feat)

## Files Created/Modified

- `web/src/components/WalkthroughStepList.tsx` - Added prKey prop, VIEWING badge, Ask button with sparkle icon, green checkmark badges for visited steps
- `web/src/components/FindingsSidebar.tsx` - Dual badge layout, activeFindingId prop, active card highlighting, validity toggle with postSessionEvent, invalid dimming + open/dismissed counts
- `web/src/components/SubmitModal.tsx` - SubmissionPanel enhanced with subp-table recap, CI status row, subp-verdict-card with Why? link
- `web/src/App.tsx` - Added prKey prop to WalkthroughStepList call site (Rule 3 fix for missing required prop)

## Decisions Made

- Used prop-passing pattern (`prKey` on `WalkthroughStepList`) consistent with existing component conventions rather than reading from store
- Added `activeFindingId` as optional prop to `FindingsSidebar` so callers can control active card without store changes — the prop is not yet wired from App.tsx (future plan task)
- Stage count in SubmissionPanel is now dynamic (counts summary/walkthrough/review completion booleans) rather than hardcoded "3 of 3"

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] Added prKey prop to WalkthroughStepList call site in App.tsx**
- **Found during:** Task 1 (restyle WalkthroughStepList)
- **Issue:** Plan added required `prKey` prop to WalkthroughStepList but App.tsx still passed the old prop signature without prKey, causing a TypeScript error
- **Fix:** Added `prKey={state.prKey}` to the WalkthroughStepList JSX in App.tsx
- **Files modified:** web/src/App.tsx
- **Verification:** TypeScript compilation succeeds with no new errors
- **Committed in:** b943b5c (Task 1 commit)

---

**Total deviations:** 1 auto-fixed (1 blocking)
**Impact on plan:** Necessary to fix the call site when the prop interface was extended. No scope creep.

## Issues Encountered

- Pre-existing TypeScript errors in `web/src/__tests__/DiffViewer.test.tsx` related to a missing `prKey` prop on `DiffViewerProps` — these are out-of-scope pre-existing failures not caused by this plan's changes.

## Known Stubs

- `activeFindingId` prop on `FindingsSidebar` is declared but not yet passed from `App.tsx` — active card highlighting won't trigger until a future plan wires the prop from the active finding selection state.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- All three right-panel components are restyled and TypeScript-clean
- FindingsSidebar validity toggle is wired end-to-end via postSessionEvent
- WalkthroughStepList Ask button is wired via postUserRequest
- SubmissionPanel CI status and stage counts are live from store
- Plan 04 (inline comment composer alignment) can proceed

---
*Phase: 06.3-walkthrough-and-review-stage-design-alignment*
*Completed: 2026-04-27*
