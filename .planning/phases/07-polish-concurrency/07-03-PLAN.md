---
phase: 07-polish-verification
plan: 03
type: execute
wave: 3
depends_on:
  - 07-02
files_modified: []
autonomous: false
requirements:
  - SESS-04
must_haves:
  truths:
    - "Full test suite passes green (server + web build)"
    - "Pitfall 3 (signal-ratio check) fires on a nit-heavy review against a real PR"
    - "Pitfall 4 (self-review stance) — default verdict is request_changes, not approve"
    - "Pitfall 5 (large-PR handling) — walkthrough on a large PR does not choke or lose context"
    - "Pitfall 12 (pre-existing code guard) — comments on unchanged lines are rejected unless flagged"
    - "Pitfall 14 (walkthrough ordering) — narrative ordering covers the core change first, not alphabetical"
    - "Auth identity badge visible in TopBar during real usage"
    - "Any papercuts found during verification are fixed before phase completion"
  artifacts: []
  key_links: []
---

<objective>
Run the full test suite for baseline confirmation, then perform the D-07 manual verification pass against a real PR, fixing any papercuts or issues discovered.

Purpose: D-05 mixed verification approach — this plan handles the "manual verification" half. D-08 says "fix whatever the verification pass surfaces." Papercut scope is emergent, not pre-planned; this plan's `files_modified` is intentionally empty because the set of files to fix depends on what breaks.

Output: All verification items confirmed working (or fixed), full green test suite, phase ready for closure.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-polish-concurrency/07-CONTEXT.md
@.planning/phases/07-polish-concurrency/07-RESEARCH.md
@.planning/phases/07-polish-concurrency/07-01-SUMMARY.md
@.planning/phases/07-polish-concurrency/07-02-SUMMARY.md
@.planning/research/PITFALLS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full test suite baseline + automated pitfall verification</name>
  <files></files>
  <read_first>
    server/src/__tests__/pitfall-verify.test.ts,
    server/src/ingest/__tests__/identity.test.ts,
    server/src/mcp/tools/__tests__/start-review.test.ts
  </read_first>
  <action>
Run the complete test suite and verify all automated pitfall tests pass:

```bash
cd server && npx vitest run --reporter=verbose
```

Verify specific pitfall coverage by confirming these tests exist and pass:
- Pitfall 1: `server/src/submit/__tests__/anchor.test.ts` — confirm at least 5 tests pass, including one that asserts `position` is `undefined`
- Pitfall 6: `server/src/http/__tests__/secure-headers.test.ts` — confirm CSP tests pass including the new `avatars.githubusercontent.com` assertion
- Pitfall 8: `server/src/__tests__/pitfall-verify.test.ts` — Pitfall 8 describe block passes
- Pitfall 9: `server/src/__tests__/pitfall-verify.test.ts` — Pitfall 9 describe block passes (both SHA-match and SHA-mismatch cases)
- Pitfall 10: `server/src/http/routes/__tests__/confirm-submit.test.ts` — 409 duplicate submission test passes
- Pitfall 16: `server/src/__tests__/pitfall-verify.test.ts` — Pitfall 16 ephemeral port test passes

Also verify the web build succeeds:
```bash
cd web && npx vite build
```

If ANY test fails, diagnose and fix the failure before proceeding to Task 2. The fix may involve any file in the codebase. Common issues to watch for:
- Import path mismatches after Plan 01/02 changes
- Mock setup gaps in existing tests that interact with the new `authenticatedUser` field on ReviewSession
- TypeScript compilation errors from the AuthIdentity type additions

Report the full test count and pass/fail ratio.
  </action>
  <verify>
    <automated>cd /Users/connorbarr/dev/personal/git-review-plugin/server && npx vitest run 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `cd server && npx vitest run` exits 0 with 0 failures
    - `cd web && npx vite build` exits 0
    - Test output shows pitfall-verify.test.ts with 4+ passing tests
    - Test output shows identity.test.ts with 7+ passing tests
    - Test output shows anchor.test.ts with 5+ passing tests
    - Test output shows secure-headers.test.ts with 9+ passing tests
    - Test output shows confirm-submit.test.ts 409 test passing
  </acceptance_criteria>
  <done>Full test suite green. All automated pitfall verification items confirmed. Web build succeeds.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Manual PITFALLS verification pass against a real PR (D-07)</name>
  <files></files>
  <action>
Human verifies the 5 manual D-07 items by launching a review session against a real GitHub PR and checking each behavior. This checkpoint covers what automation cannot: subjective quality judgments about LLM behavior and visual correctness.

Steps for the human verifier:

1. Auth identity badge (D-02/D-03/D-04): Verify the TopBar shows avatar (20px circle) + username in row 1 right side next to Settings. If GITHUB_TOKEN is set to a different account, verify warning icon with tooltip "gh auth and GITHUB_TOKEN resolve to different users". Refresh browser and confirm badge survives.

2. Pitfall 3 — Signal-ratio check: Review a PR where nit-level findings dominate. Before submission, verify the pre-submit signal-ratio check warns about nit floods (> 3 nits or signal ratio less than 40%).

3. Pitfall 4 — Self-review adversarial stance: Run self-review on a PR. Verify the default verdict is "Request changes", not "Approve". The LLM should argue down from request_changes, not up from approve.

4. Pitfall 5 — Large-PR handling: Review a larger PR (20+ files if possible). Verify the walkthrough loads without choking — no context exhaustion, no missing hunks.

5. Pitfall 12 — Pre-existing code guard: During walkthrough, verify that comments on unchanged context lines are rejected unless explicitly flagged with pre_existing.

6. Pitfall 14 — Walkthrough ordering: Verify the walkthrough narrative covers the core change files first, not snapshot/lockfile/test churn. Ordering should be intent-aware.

If issues are found: fix them before marking approved (per D-08 papercut repairs). The fixes are emergent — any file may be modified.

Resume signal: Type "approved" to complete Phase 7, or describe specific issues that need fixing.
  </action>
  <verify>Human confirms all 6 manual verification items pass or issues have been fixed.</verify>
  <done>All D-07 manual verification items confirmed working. Any papercuts found have been fixed and committed. Phase 7 is ready for closure.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Manual verification plan introduces no new trust boundaries |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-08 | (meta) | Manual verification | accept | Subjective checks require human judgment. The 5 manual items from D-07 are not automatable because they depend on real PR content and LLM behavior. Formal pass/fail criteria are intentionally loose per CONTEXT.md ("no need for formal VERIFICATION.md ceremony"). |
</threat_model>

<verification>
```bash
# Pre-checkpoint automated verification:
cd /Users/connorbarr/dev/personal/git-review-plugin/server && npx vitest run --reporter=verbose
cd /Users/connorbarr/dev/personal/git-review-plugin/web && npx vite build

# Post-checkpoint: if papercuts were fixed, re-run:
cd /Users/connorbarr/dev/personal/git-review-plugin/server && npx vitest run
```
</verification>

<success_criteria>
1. Full server test suite passes (0 failures)
2. Web build succeeds
3. Human verification of all 6 manual D-07 items: approved or issues fixed
4. Auth identity badge visible and functional during real-PR verification
5. Any papercuts found during verification are fixed and committed (per D-08)
6. Phase 7 is ready for closure
</success_criteria>

<output>
After completion, create `.planning/phases/07-polish-concurrency/07-03-SUMMARY.md`
</output>
