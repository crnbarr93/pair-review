---
phase: quick
plan: 260429-hp9
type: execute
wave: 1
depends_on: []
files_modified:
  - web/src/App.tsx
autonomous: true
---

<objective>
Fix the review page DiffViewer to show all changes instead of walkthrough-filtered changes, so findings can be scrolled to regardless of walkthrough scope.
</objective>

<tasks>
<task type="auto">
  <name>Make virtualList and filteredDiff step-aware</name>
  <files>web/src/App.tsx</files>
  <action>
Add `activeStep` condition to both `virtualList` and `filteredDiff` useMemo hooks so walkthrough filtering only applies on the walkthrough step. Review and submission steps always get the full diff.
  </action>
  <verify>
    <automated>cd web && npx vite build</automated>
  </verify>
  <done>DiffViewer shows all changes on review/submission steps, walkthrough-filtered on walkthrough step only.</done>
</task>
</tasks>
